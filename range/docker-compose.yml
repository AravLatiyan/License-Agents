services:
  mailpit:
    image: axllent/mailpit:latest
    container_name: universal-imports-mailpit
    ports:
      - "8025:8025"
      - "1025:1025"

  fake-portal:
    build:
      context: ./fake-portal
    container_name: universal-imports-fake-portal
    ports:
      - "8080:8080"