# UNIVERSAL IMPORTS — PLAN & LIVE STATUS
> **Nothing gets in without clearance.**
> The Agent Harness Hackathon · WeMakeDevs × TrueFoundry × Qodo · 24–30 Aug 2026
> **Deadline: 30 Aug 20:00 London = 31 Aug 00:30 IST. We submit 30 Aug, 18:00 IST.**

**This is the only planning file. Everything lives here: the plan, the task board, progress,
decisions, errors, and the traps. Claude Code reads and updates it every session.**

---

# ⚡ HOW THIS FILE WORKS

1. **§1 LIVE STATUS** is the top of every session. Claude reads it first and reports it back.
2. **§2 NEXT UP** always holds **exactly 4 tasks**. Claude presents them as a numbered menu and
   waits for you to pick one. Never more than one at a time.
3. When a task finishes: Claude moves it to **§4 DONE** with a result line, deletes it from
   NEXT UP, pulls replacements from **§11 BACKLOG**, and shows the new menu.
4. Anything learned goes into **§5 BLOCKED**, **§6 DECISIONS**, or **§7 ERRORS** as it happens —
   never batched for later.
5. If the plan turns out to be wrong, it goes in **§8 SUGGESTED CHANGES**, gets raised at
   standup, and someone edits the plan sections below. **The plan is allowed to change; drifting
   silently is not.**

Sections §1–§8 are living. Sections §9–§18 are the reference and change only by decision.

---

# §1 LIVE STATUS

```
PHASE:        DAY 1 — SLICE 1 (ugly vertical slice) — **stale, see note below**
DATE:         2026-08-27
DAYS LEFT:    3
ACTIVE:       Owner 1 (Harness) · Owner 2 (Tools) · Owner 3 (Cockpit) · Owner 4 (Mission — T-060 merged, see LAST UPDATED)
DORMANT:      nobody currently — confirmed against live GitHub PR state, not assumed from PLAN.md's own prior text
TODAY'S GOAL: Slice 1 running end to end, ugly (O1) — three subagents now defined in agent.json, instructed to report structured evidence per T-024 (prompt guidance merged, runtime behavior still unverified — see BLOCKED ON). `tools/domain-intel` (parse_message, normaliser, domain_intel, url_reputation — T-003/T-004/T-010/T-011/T-012/T-020/T-021, all O2) now merged into `main` (see LAST UPDATED) — Slice 1/2's O2 read-only tool work is fully integrated; `correspondence_history` (T-022) is **blocked** (§2/§5) on an explicit O2/O4 mailbox-protocol decision, not freely pickable. Contracts + Cockpit scaffold (T-050) already on main, consuming the mission fixture end to end (O3). T-060 (Range: Mailpit + fake portal + seed.sh) merged 2026-08-28 (PR #22).
BLOCKED ON:   T-015 live-fire test — no model provider configured; T-023/T-024 runtime verification — actual delegation and reporting *behavior* unobserved, not just JSON acceptance; T-022 — needs an explicit O2/O4 decision between Mailpit's HTTP API and a separate IMAP-capable service (§5) — **not** "add an IMAP port to Mailpit," which is impossible, not just unconfigured; T-035 — no Daytona API key configured, so no sandbox exists to run detonation in, and the "snapshot" half of the task is unsupported by the platform (§5/§8); T-055 — no persisted-past-missions data source exists (T-040 unstarted, no confirmed TrueForge session-history API), and CLAUDE.md bars building one ourselves (§5); T-032 — `create_block_rule` has no decided destination and nothing consumes block rules (§5/§8)
LAST UPDATED: 2026-08-29 16:05 · **T-032 investigated read-only and logged as BLOCKED BY SCOPE — documentation only, no code, T-032 NOT implemented.** Its contract half is already finished (signature in §10, `ProposedActionName` in `contracts/events.ts`, and one of T-034's four gate names in `agent.json`), so this is **not** an invalid premise like T-025 — the task is real. What is undecided is *where a block rule lives*: unlike T-030 ("over IMAP") / T-031 ("over SMTP") / T-033 ("to the RDAP abuse contact"), T-032 names no transport at all; Mailpit exposes **no** rule/filter endpoint (checked in the vendored spec); no rule store exists in the repo and no `tools/` module writes to disk; and **nothing reads block rules**, so implementing it now would invent both a storage format and a consumer-less store. Deliberately **not** decided: no storage format, API, enforcement behaviour or demo semantics invented — a local `imports-mcp`-owned store is noted in §8 as one plausible future option needing explicit project approval, not adopted. Distinct from the T-022/T-030 mailbox-protocol blocker: a pattern-based rule needs no mailbox. Logged in §5 and §8; T-032 stays in §11 flagged ⛔ and fully recoverable. T-030/T-031/T-033 untouched. Previous entry: 2026-08-29 15:30 · **T-040 investigated and resolved without code (§4) — session persistence and resume are already provided by TrueForge, so there was nothing to build and building it would have broken CLAUDE.md's don't-rebuild rule.** Verified against a live instance by response, not docs: `GET /api/v1/sessions` → HTTP 200, paginated, "List sessions"; `/sessions/{id}` and `/sessions/{id}/events` real; `/turns`, `/turns/{id}/events`, `/turns/{id}/subscribe` all present; and `db.sqlite` survived a full restart with the server serving again. This **corrects an earlier O1 call** that T-040 was an invalid premise for lack of such an API — the API exists; the task's *framing* was wrong, not its goal. **Unblocks part of O3's T-055** (§5): its reason #2 ("no session-listing/history endpoint has ever been confirmed... nothing has checked") is now disproven; its other reasons stand and remain O3's call. The "campaign dossier" half stays undefined and was deliberately not invented (`dossier`/`campaign` appear nowhere in TrueForge's spec, no §10 tool, no §17 beat) — logged as a scope question in §8. No code written; T-040 annotated in §11 rather than deleted or falsely marked complete. Previous entry: 2026-08-29 12:10 · **T-033 (O1, disclosed takeover of an O2 §11 item) merged into `main` as `71107ad33b22f793f022504dd0e91dab8fbb2696` (PR #40).** `file_abuse_report(domain, evidence)` now exists — the second of the four T-034 gate-target tools, so two of four gates can now reach a real tool. Resolves the abuse mailbox from RDAP via the merged `domain_intel`; `contracts/` untouched (the signature was already pinned by §10 and the fixture). Shared SMTP machinery extracted into `tools/imports_mcp/_smtp.py` rather than duplicated, with T-031's 43 tests passing untouched as the regression proof. Refuses to send unless the SMTP destination is loopback unless `ALLOW_EXTERNAL_SMTP=1` is set — this tool mails a *real registrar's* abuse mailbox, so CLAUDE.md trap #6 gets a hard guard, not just a default. Qodo found 3 real bugs (non-string RDAP contact crashing the tool; a DNS-rebinding window between the guard and the connect; a live test hitting production `rdap.org`), all fixed and re-reviewed clean of Bugs on the exact merged HEAD. `tools/` 198 passed / 3 skipped, `harness` 18/18. **No real registrar or abuse mailbox was contacted** — live proof was a real SMTP send over a loopback socket only; the Mailpit test could not run (Docker daemon down). T-030/T-032 remain O2's and unstarted. Previous entry: 2026-08-29 01:23 · **T-056 (O3) merged into `main` as `4c83f5caa919711608209a38efccad3c9201609c` (PR #47) — resumable-stream reconnect handling, deliberately scoped to just the confirmed `after_sequence_number` resume mechanic, not the full live `trueForgeEventSource` (needs a translation layer from TrueForge's raw turn-stream to `mission.*` events that nothing has ever defined — logged in §8, not guessed at).** Qodo reviewed clean (0/0/0). **Cross-owner discovery while picking the next task: T-036 (LICENCE REQUIRED panel) has been sitting unpicked in §11's Slice 3 this whole time** — outside the "from join day" Cockpit list every O3 session had been refilling from exclusively. Only its presentation half is genuinely unblocked (fixture already has real approval events); the live-submission half is blocked on the same translation layer, just above — corrected in §2/§4/§8/§11 after Qodo caught this entry's first draft overclaiming "fully unblocked." Picked next for the presentation half, squarely in §17's demo script (1:50-2:20), ahead of T-043/T-044. §2 refilled with T-036; §11 cleaned of T-036 (and still carries T-055, marked blocked). Previous entry: 2026-08-29 01:10 · **T-055 investigated read-only and logged as BLOCKED — documentation only, no code, T-055 NOT implemented.** Its data dependency (persisted past missions, cross-mission campaign linkage) doesn't exist anywhere in this codebase: T-040 (O1, session persistence) is still unpicked; TrueForge's only confirmed live API (T-002) covers single-session/turn mechanics, not a session-history listing; §10's "write campaign dossier" names no tool and no storage format; §17's demo script has no dossier beat at all. CLAUDE.md explicitly bars rebuilding session persistence ourselves, so no localStorage/database workaround was built and no undocumented API was guessed at. Full reasoning in §5. Swapped for **T-056** (Reconnect handling) in §2 — its resumable-subscribe API is already confirmed real and already named as Cockpit's planned path (T-002/§6, `missionSource.ts`'s own docstring), so it's genuinely buildable now. T-055 moved back to §11, marked blocked, not left occupying the NEXT UP slot indefinitely. Previous entry: 2026-08-29 00:54 · **T-054 (O3) merged into `main` as `5431e70a797960fdb275dbec22b22e059933d1c3` (PR #43) — verdict panel now renders live, the demo's plain-language judgment moment (§17, 2:40-3:00).** Qodo caught 2 real accessibility Bugs: the suspicious-verdict label's plain `orange` text failed WCAG contrast (~1.97:1, fixed to `#b45309`, ~5:1/~4.2:1 on white/black); the verdict's async arrival had no live region for screen readers (fixed with `role="status"`). Both fixed, re-reviewed clean (0/0/0). **Also merged: PR #31 (T-031's PLAN.md completion sync, `579cf3f`)** — held open since 2026-08-28 per explicit instruction while other work landed; resolved by threading T-031's §4/§1 entries into their correct chronological position rather than just conflict-resolving, and Qodo caught 1 real factual bug in the inserted text (falsely claimed T-031 "removed one of T-015's two blockers" — T-015's own §5 blocker is only the missing model provider key, unrelated to which of T-034's four gate-target tools exist; corrected in both §1 and §4). §2 refilled with T-055; §11 cleaned of T-054. Previous entry: 2026-08-29 00:39 · **T-053 (O3) merged into `main` as `94f90ea60f2418acddd9c75aaf1e0e3bd17e7104` (PR #41) — detonation panel (redirect chain, forms, summary) now renders live.** Screenshot half deliberately left as an honest "not available" state, not faked — no capture or serving mechanism exists yet (T-001/T-035, §5). Qodo caught 1 real Bug: `events.find()` (first match) instead of `findLast()` (current/latest) in both the panel and `missionPlan.ts`'s `detonationEvent` lookup — a retried detonation would've stuck on the stale first result. Fixed, re-reviewed clean (0/0/0). Same latent bug found in `messageEvent`/`verdictEvent` lookups, deliberately not fixed (out of scope) — logged in §7 for whoever picks it up. §2 refilled with T-054; §11 cleaned of T-053. Previous entry: 2026-08-29 00:20 · **T-052 (O3) merged into `main` as `449d9e12e075f111ba52e61278a58c4c57f1ea05` (PR #36) — the three evidence lanes now render live, full typed fields per lane, not T-051's collapsed one-line join.** `missionPlan.ts`'s lane derivation is now a shared export (`buildEvidenceLanes`) so the plan tree and this new panel can't disagree. Qodo caught 1 real Bug: an empty lane stayed `pending` forever even after the mission finished — actually a pre-existing bug in T-051's own original code, never caught then because the fixture always populates every lane (§7 has the lesson: a Qodo-clean review only covers the paths the diff/fixture actually exercises). Fixed and re-reviewed clean (0/0/0). §2 refilled with T-053; §11 cleaned of T-052. Previous entry: 2026-08-29 00:03 · **T-051 (O3) fully merged into `main` — Mission View is a plan tree, not T-050's flat event log.** Landed as two PRs after being split to fix a real 400-line violation: `cockpit/src/missionPlan.ts` (PR #34, `e5b06fa`) then `MissionView.tsx`/`index.css`/`PLAN.md` rebased on top (PR #32, `7db05f7`). 4 real Qodo Bugs found and fixed across both PRs' review rounds (gate state silently dropped resumed-stream events; empty `result_summary` stayed `active`; status was inaccessible to screen readers; `approval_resolved.reason` was captured but never shown) — full writeup in §4. One stale Qodo finding (post-rebase PR-size count) corrected via PR comment rather than an in-thread dismiss, since no thread existed for it this time (§7). §2/§11 already refilled with T-052. Previous entry: 2026-08-28 23:45 · **T-051 (O3) implemented — Mission View is now a plan tree, not T-050's flat event log.** New `cockpit/src/missionPlan.ts` derives §10's six architecture stages (message → evidence [3 fixed lanes] → detonation → verdict → 4 licence gates → complete) purely from the `MissionEvent` stream — every stage/lane/gate slot renders `pending` from event 0, `active` once its own events land, `done` once the stream moves past it; a denied gate gets its own `denied` status per §17. `MissionView.tsx`/`index.css` updated to match; `App.tsx`/`missionSource.ts`/`useMissionEvents.ts` untouched. `npm run build` clean; tree logic verified against the real fixture at 10 checkpoints plus a forced-denial run (§4 for the full writeup) — in-browser rendering not verified, no browser tool available this session. §2 refilled with T-052; §11 cleaned of both T-050 and T-051. Previous entry: 2026-08-28 23:16 · **T-031 (O1, cross-owner takeover from §11's O2 listing) implemented and merged into `main` as `6550c20051de7c7899bb9b5a0657231f6984da73` (PR #29).** `notify_impersonated` is the first of T-034's four gate-target tools to actually exist. 3 real Qodo findings (blank `SMTP_HOST` no-op'd every send; `SMTP_PORT=0` silently reached smtplib's real default port 25 instead of the Range; `smtp_host` unbounded and uncapped) fixed and re-scanned clean; the 4th (PR size) documented and dismissed, not split — full reasoning in §4. **T-030/T-032/T-033 still unbuilt, untouched.** No `tool.approval_required` event has been observed yet — T-015's own blocker is unchanged by this (§5 names exactly one: no model provider key configured; T-015's live-fire test uses its own separate `quarantine_stub`, unrelated to which of the four real product gate-target tools exist). What changes is that one of T-034's four gate-target tools (`notify_impersonated`) is now real, not just configured. §11 refilled by removing the now-done T-031 line; §2 untouched (T-031 was never listed there — it went straight from §11 to a PR as a disclosed takeover). Previous entry: 2026-08-28 22:34 · **T-035 investigated read-only and logged as BLOCKED — documentation only, no code, T-035 NOT implemented.** Verified against the live TrueForge OpenAPI schema and live endpoints (not inferred): Daytona *is* TrueForge's sandbox provider and its `api_key` is required, but no provider is configured (`GET /api/v1/settings/sandbox-providers` -> 404) and no key exists anywhere in the project — so `sandbox.enabled: true` yields no sandbox and nothing can be live-verified. TrueForge exposes **no** snapshot/warm-pool/prewarm mechanism (`warm`/`pool`/`prewarm` absent from the spec; both `snapshot` hits are unrelated session-binding text); the only supported lever is `auto_stop_interval_in_minutes`, a tenant setting with nowhere in the repo to commit it. "The Daytona sandbox job" that T-026 depends on is **defined nowhere**. Logged in §5 (blocker) and §8 (scope correction); T-035 stays in §2 flagged ⛔ as a real future task — blocked, **not** disproven like T-025. Nothing else touched: `detonate.js`, `agent.json`, T-026, T-022, T-051, T-060, T-061 all unchanged. Previous entry: 2026-08-28 21:58 · **T-034 (O1) implemented and merged into `main` as `7e4fade2ac41e7d47120b1872ff805f3ad8d3462` (PR #26).** `harness/agent.json`'s `mcp_servers` went from `[]` to the `imports-mcp` connector with `require_approval_for_tools` = the four licence gates (`quarantine`/`notify_impersonated`/`create_block_rule`/`file_abuse_report`, §10's order). Configuration only — **T-030–T-033 are still unbuilt**, so the gates are proven *accepted* but have never *fired*: no `tool.approval_required` event has been observed, and none can be until those four tools exist and a model provider key is configured (§5). Verified live against a real TrueForge (WSL2) that the platform does **not** reject gate names for tools that don't exist yet, with two deliberate 400-rejecting controls proving manifest validation genuinely runs; the real `harness/agent.json` was then POSTed live and schema-accepted (expected 422 provider-not-configured). Qodo caught 1 real Bug — the prompt's old "propose the action and wait" would have stopped the model *before* the gated tool call, preventing the very gates this task configured from firing — fixed and re-reviewed clean (0/0/0) on the exact merged HEAD. §2 refilled with T-035. Previous entry: 2026-08-28 21:27 · **PLAN.md-only correction, no code — fixes Qodo's PR #23 review (2 Bugs) plus a merge conflict against `main` that had moved past PR #23's base.** (1) T-060 merged since PR #23 opened (`f9c18f6`, 2026-08-28) — re-verified directly against `main`'s actual `range/docker-compose.yml`: Mailpit `v1.31.0` pinned, ports `8025`/`1025` published, no IMAP port, no auth anywhere in the compose file or the vendored `range/mailpit-api.json` spec. (2) Qodo bug #1: the prior §5 row offered "add an IMAP port to PR #22" as a fix path — wrong, not just premature. Mailpit has no IMAP server implementation at all (its own maintainer says so directly, `github.com/axllent/mailpit/issues/72`; its official docs list only SMTP/web-UI/REST-API). Corrected §5's row to state this plainly and replaced the false option with the two real ones: (a) target Mailpit's existing HTTP API, (b) bring in a separate IMAP-capable service — neither chosen here. (3) Qodo bug #2: T-022 was still a plain, unmarked, freely-pickable §2 menu row despite being blocked in §5. Marked it `⛔ BLOCKED, do not pick` directly in the row, left it in the menu (not deleted) so the block stays visible where a session would see it. (4) Merge conflict: PR #23's branch was behind `main` by the T-060/T-025/T-034 merges (PRs #22/#20/#25) — merged current `main` in, resolved the one PLAN.md conflict (§2's note stack, purely additive, all three notes — mine plus O1's two T-025/T-034 notes — kept, nothing dropped). Verified before writing: `git diff --stat` shows only `PLAN.md` touched; no conflict markers remain; all 19 section headers present exactly once; `pytest` from repo root — 104 passed, 1 skipped, unchanged
```

> Note (2026-08-27, O3): PHASE above says "DAY 1 — SLICE 1" but today is Day 3 per §14's own schedule and Slice 1's tasks are all done — flagging the staleness rather than silently reassigning it, since declaring the project's actual current slice/phase is a bigger call than T-050's scope covers (Slice 2's T-022/T-026 are still open — T-024 done, §4 — so "Slice 3" isn't clearly correct either). Whoever next does cross-owner bookkeeping should settle this.

## 🚨 DO THIS FIRST — before any task below
- [ ] Repo **public** on GitHub, MIT licence
- [ ] **Qodo installed on the repo** — `app.qodo.ai/signin` → link GitHub → install on this repo.
      **Before the first real commit.** ~10 minutes, then zero ongoing effort. See §15a for why
- [ ] `main` protected: require a PR, require one review
- [ ] Everyone **registered** for the hackathon (free, one form)
- [ ] Everyone **starred `github.com/truefoundry/trueforge`** (free prize draw)
- [x] `node --version` ≥ 22 · `npx @truefoundry/trueforge` → `localhost:8790` — **on Windows, run this from WSL2 with Node installed inside the distro, not native Windows (segfaults). See §7**
- [ ] Model provider configured · **hard spend cap set on the API key**
- [ ] Everyone has opened **at least one PR today**

---

# §2 NEXT UP — PICK ONE

> Claude: present these as a numbered menu, wait for a choice, then work only on that one.

| # | ID | Task | Owner | Size | Why now |
|---|---|---|---|---|---|
| 1 | **T-036** ⚠️ | LICENCE REQUIRED panel — **presentation half only, buildable now; live Allow/Deny submission blocked, see below** | O3 | — | T-056 done (§4) — swapped for **T-036**, found sitting unpicked in §11's Slice 3 while selecting the next task. **Correction (Qodo, PR #48):** an earlier version of this row overclaimed "depends only on T-002, fully unblocked" — wrong, and inconsistent with §8's own new row about the undecided TrueForge→`mission.*` translation layer, logged in this same PR. Only the **presentation half** is genuinely unblocked: `contracts/fixtures/mission-happy-path.json` already carries real `mission.approval_required`/`mission.approval_resolved` events for all 4 gates, so the request panel + Allow/Deny buttons can be built and demoed against real fixture data, same pattern as T-051-054/T-056. The **live-submission half** (a button actually resuming a real TrueForge turn via `user.tool_approval`) is blocked on the same undecided translation layer §8 already records — no real event source exists to submit to. Squarely in §17's demo script (1:50-2:20) for the presentation half; CLAUDE.md's qualifying test ("stops for a person") is satisfied by the UI existing and being demoable, not by a live backend connection existing today |
| 2 | **T-022** ⛔ | `correspondence_history` — **BLOCKED, do not pick** — mailbox protocol undecided (§5) | O2 | — | Last of the three planned Slice 2 read-only `imports-mcp` tools; `domain_intel`/`url_reputation` (T-020/T-021) are already done on `tools/domain-intel` — but its own task name ("IMAP search") no longer matches what T-060's merged Mailpit can do (§5) |
| 3 | **T-035** ⛔ | Warm-sandbox strategy + wire `detonate.js` into the sandbox — **BLOCKED, do not pick** — no Daytona API key, and the "snapshot" half is unsupported (§5/§8) | O1 | — | Investigated 2026-08-28 (read-only, against the live TrueForge schema): Daytona is the real sandbox provider but **no key is configured**, so no sandbox exists to run in; TrueForge exposes **no** snapshot/warm-pool mechanism (only `auto_stop_interval_in_minutes`, a tenant setting); and "the sandbox job" T-026 depends on is defined nowhere. Left in this menu so the blocked status is visible where a session would see it — a real future task, blocked and needing scope clarification, **not** disproven like T-025 |
| 4 | **T-026** | `detonate` MCP wrapper dispatching O1's sandbox job | O2 | — | Wires the already-working `harness/detonate.js` (T-014) into the MCP tool surface so the INFRASTRUCTURE subagent (T-023) can actually call it |

> Note (2026-08-26, O1, replacing the prior table): all four previous entries (T-016, T-010, T-011, T-012) are done and merged into `main` — confirmed via `gh pr view`/`git merge-base`, not assumed. Refilled from §11 per the existing selection rules (current slice first, then what unblocks other owners, then demo requirements): T-050 unblocks O3 (idle since joining, and §17's demo has nothing to show without a cockpit); T-022/T-026 are Slice 2's remaining current-slice work (T-024 done, §4). T-025 (cheap/strong model split) stays in backlog — a cost/config tweak, not blocking anything. `tools/domain-intel` (T-020+T-021, both done) still needs to merge up into `main` — not listed here since that's integration bookkeeping, not a backlog task.
> Note (2026-08-27, O3): T-050 done (§4) — swapped for T-051 in the same slot, per §11's Cockpit backlog order. Did not touch T-022/T-024/T-026 rows; no evidence either owner's status changed.
> Note (2026-08-27, O1): T-024 done (§4) — swapped for T-025 in the same slot, the next O1 Slice 2 backlog item in order. Also removed T-024 from §11 — it had been sitting in both tables since being picked, a pre-existing duplicate this cleans up as a side effect of finishing it, not a separate cleanup pass. Did not touch T-022/T-026/T-051 rows.
> Note (2026-08-27, cross-owner sync, corrected 2026-08-28 — see §5): before picking **T-022**, read the §5 row — it is now formally **blocked**, not just flagged, pending an explicit O2/O4 decision on which mailbox protocol `correspondence_history` actually speaks. T-022 is intentionally left in this menu (not deleted) so the blocked status stays visible where a session would actually see it, but should not be picked until §5 is resolved.
> Note (2026-08-27, O1, correction): T-025 investigated and disproven — `dynamic_sub_agents` has no model-selection field (verified against `harness/agent.json`'s actual schema and TrueForge's own current docs, `trueforge.dev/create-agent/overview`'s full `manifest.config` field table: `dynamic_sub_agents` accepts only `{enabled: bool}`; `model` exists once, at the manifest top level, for the root agent only). Not a blocked task — not buildable at all. Swapped for **T-034** in this slot (§7/§8 for the full writeup). Did not touch T-022/T-026/T-051 rows.
> Note (2026-08-28, O1, T-034 done): T-034 implemented and merged into `main` (§4, PR #26, `7e4fade`) — swapped for **T-035**, the next O1 item in §11's order, and removed from §11. The configuration is live-accepted; the gates themselves still cannot fire until T-030–T-033 exist, so **that dependency did not go away, it moved** — it's now recorded in §4 and §5, not here. Did not touch T-022/T-026/T-051 rows.
> Note (2026-08-28, O1): T-034's description expanded, not implemented — a readiness investigation (read-only, against current `main`) found the one-line version understated real scope. `agent.json`'s `mcp_servers` is `[]`, not partially configured, so this task also has to add the connector itself, not just flip a flag on an existing entry; and none of the four gate-target tools exist in `tools/imports_mcp/server.py` yet, so the gates can be written now but can't be meaningfully live-verified until T-030–T-033 exist. T-034 itself is legitimate and schema-supported (T-015 already proved `require_approval_for_tools` live) — this is a scope/dependency correction, not a T-025-style invalidation.
> Note (2026-08-28, O3): T-051 done (§4) — swapped for T-052 in the same slot, the next O3 Cockpit backlog item in order (§11). Also removed T-050 and T-051 from §11 — both had been sitting there since being picked (T-050 done since 2026-08-27, T-051 just now), a pre-existing duplicate the same shape as T-024's (§2, 2026-08-27 note) — cleaned up as a side effect of finishing T-051, not a separate pass. Did not touch T-022/T-026/T-035 rows.
> Note (2026-08-28, O3): T-052 done (§4) — swapped for T-053 in the same slot, the next O3 Cockpit backlog item in order (§11), also removed from §11. Did not touch T-022/T-026/T-035 rows.
> Note (2026-08-28, O3): T-053 done (§4) — swapped for T-054 in the same slot, the next O3 Cockpit backlog item in order (§11), also removed from §11. Did not touch T-022/T-026/T-035 rows.
> Note (2026-08-28, O3): T-054 done (§4) — swapped for T-055 in the same slot, the next O3 Cockpit backlog item in order (§11), also removed from §11. Did not touch T-022/T-026/T-035 rows.
> Note (2026-08-29, O3): T-055 investigated, not implemented — found genuinely blocked (§5), not disproven like T-025: the task is legitimate and will become buildable once T-040 (or a confirmed TrueForge history API) exists. Swapped for **T-056** in the same slot. T-055 moved back to §11 (Cockpit backlog), marked blocked, rather than left occupying this slot indefinitely — §5 carries the full reasoning. Did not touch T-022/T-026/T-035 rows.
> Note (2026-08-29, O3): T-056 done (§4) — swapped for **T-036**, discovered sitting unpicked in §11's Slice 3 the whole time (not the "from join day" Cockpit list this and every prior O3 session had been refilling from exclusively), removed from that list now that it's picked. Prioritized ahead of T-043/T-044 (later in the backlog order) because its presentation half is demo-critical and genuinely buildable now against real fixture data — **correction (Qodo, PR #48): only that presentation half is unblocked, not the whole task; the live Allow/Deny submission half is blocked on the same undecided TrueForge→`mission.*` translation layer §8 records, T-036's own row now says so explicitly.** Did not touch T-022/T-026/T-035 rows.

⏱ = has a timebox. See §3.

---

# §3 IN PROGRESS

> One row per active owner. Claude fills `Started` from `date` and checks elapsed time
> every few turns.

| ID | Owner | Started (IST) | Timebox | Fallback if it expires |
|---|---|---|---|---|
| _(none yet)_ | | | | |

> Note (2026-08-26): T-010 (the previous row) shipped and merged into `main` long ago (§4) — this table wasn't cleared when it finished. No other owner has evidence of an actively-timeboxed task right now.

### Timebox rules — Claude enforces these, not you
| Task | Box | Fallback |
|---|---|---|
| T-001 chromium in Daytona | **3h** | **Text-mode detonation** — HTTP redirect chain, HTML parse, form-target extraction, "this page asks for a password and posts it to a different domain." Still genuine sandboxed work. Screenshot becomes a stretch goal |
| T-002 approvals over the API | **2h** | Cockpit renders everything else (evidence lanes, detonation, verdict); the approval moment is filmed in TrueForge's own chat UI. Ugly for the UI track — try hard before accepting this |
| T-003 intel APIs | **2h** | Cache crt.sh hard; treat missing RDAP fields as *"not published"* rather than an error |
| Any single bug | **90 min** | Stub it, log it in §7, move on, return later |

**Do not negotiate with a timebox.** You agreed to these while calm. Every hour this project
loses will be lost to refusing a fallback, not to the work being too hard.

---

# §4 DONE

> Append-only. Format: `YYYY-MM-DD · T-XXX · [Owner] · what shipped — result`

2026-08-25 · T-013 · [O1] · `harness/agent.json` + `harness/README.md` — manifest for `POST /api/v1/agents` (model, instructions, sandbox+dynamic-subagents config), schema verified against trueforge.dev docs. `mcp_servers` left empty, `imports-mcp` not built yet (T-012) — result: written, not yet runtime-verified against a live server (see §7, Windows segfault)
2026-08-25 · T-017 · [O1] · Node installed inside WSL2 Ubuntu (NodeSource 22.x); TrueForge runs clean from there, no segfault, `curl` got HTTP 200 — result: `harness/agent.json` POSTed to the live `/api/v1/agents`, schema fully accepted, only rejection was the documented 422 "model provider not configured" (expected on a fresh instance, separate checklist item). T-013 now counts as runtime-verified. T-002 unblocked
2026-08-25 · T-002 · [O1] · **SPIKE 2 answered: YES, the HTTP API fully surfaces approvals — no chat UI needed.** Confirmed straight from the live server's own OpenAPI schema (not just docs): `POST /sessions/{id}/turns` streams SSE `TurnStreamingEvent`s; a gated tool call emits `tool.approval_required` (`type, id, created_at, thread_id, tool_calls[]`). Cockpit resumes by posting a turn with `input: [{type:"user.tool_approval", thread_id, tool_call_id, approval:{status:"allow"|"deny", reason?}}]`. `/turns/{id}/subscribe?after_sequence_number=` gives resumable SSE for free (reconnect handling, §10). Full details in §6 — result: cockpit's core interaction is fully buildable, T-036 unblocked
2026-08-25 · T-014 · [O1] · `harness/detonate.js` — text-mode detonation: follows redirects (manual, capped at 10 hops, non-http(s) schemes refused), parses final HTML with `node-html-parser` (never regex, per §12), flags forms that ask for a password and post cross-domain. Self-tested against a local-only fixture server (`harness/detonate.test.js`, `node --test`, 3/3 pass) — never touched a real domain (§13). `harness/package.json` added (one dependency: `node-html-parser`); root `.gitignore` added (`node_modules/` wasn't excluded before) — result: returns `{url, redirect_chain, final_url, forms, summary}`, matches §10's `detonate(url)` shape minus the screenshot field
2026-08-25 · T-015 · [O1] · One approval gate wired end to end, up to the point that needs a real model call. `harness/stub-mcp-server.js` — throwaway Streamable HTTP MCP server (TrueForge only supports `type: "remote"`/URL connectors, no local/stdio — confirmed against its own OpenAPI schema), one tool `quarantine_stub`. Registered live with TrueForge (`POST /settings/mcp-servers`, 201), tool discovered live (`GET /mcp-servers/{name}/tools`, 200, correct schema), and a test agent referencing `require_approval_for_tools: ["quarantine_stub"]` was accepted (`POST /agents`, only rejection was the expected "model provider not configured" 422) — result: **the gate wiring itself is proven correct**; the live-fire test (an actual `tool.approval_required` SSE event from a real turn) is deferred, see §5. `harness/agent.json` itself untouched — the stub connector only exists in a throwaway test payload, never the product config
2026-08-25 · T-014 (Qodo pass) · [O1] · Fixed 5 real bugs Qodo's PR #3 review found: fetch/DNS/timeout/malformed-redirect failures now return `{url, redirect_chain, error}` instead of throwing; one malformed form `action` no longer aborts analysis of the rest of the page (represented with `action_invalid: true` instead); HTML content-type check is case-insensitive; response body is read with a hard byte cap (`readBodyWithLimit`, streamed, never fully buffered) before parsing; non-HTML success responses now return the full documented shape including `summary`. 10/10 tests pass (7 new regression tests added). Declined one finding — see §8
2026-08-25 · T-003 · [O2] SPIKE 3 — all three intel sources confirmed live: RDAP via `rdap.org/domain/{domain}` (follow redirects) returns registration date + abuse contact; crt.sh reachable but 502ing on every attempt right now (matches known trap, mitigation unchanged); URLhaus `/v1/host/` and `/v1/urls/recent/` both confirmed working with the `Auth-Key` header already present in `.env` — added `.env.example` since none existed
2026-08-25 · T-004 · [O2] Read `bring-your-own-mcp` cookbook example (README, agent.json, mcp-server.mjs, package.json) end to end — settles trap #1 (transport mismatch) before any MCP code gets written
2026-08-26 · T-016 · [O3] · `contracts/events.ts` — TrueForge wire-level approval/session types copied verbatim from T-002's confirmed schema (§6), MCP tool result shapes from §10's tool table, plus a `MissionEvent` discriminated union mapping one variant onto each stage of §10's architecture diagram (message received → per-lane evidence → detonation → verdict → 4 sequential licence gates → action executed → complete), so Cockpit's T-050/T-036 have a single stream to bind to. `contracts/fixtures/mission-happy-path.json` — one full 20-event BEC/invoice-fraud mission (fictional "Northgate Trust" domain, per §13 rule 5) matching §17's demo content, all four gates granted — result: type-checked clean with `npx -p typescript tsc --noEmit --strict contracts/events.ts` (repo has no TypeScript toolchain of its own yet — none of T-050's tooling exists, so this used a throwaway npx run, nothing added to the repo); JSON fixture validated with `python3 -m json.tool` and by hand-checking every event against the `.ts` shapes. T-050 (Cockpit scaffold) now unblocked, pulled into §2 NEXT UP in its place. PR #12 opened same day
2026-08-26 · T-016 (Qodo remediation, round 1) · [O3] · Qodo's PR #12 review found 4 real bugs and 2 rule findings — fixed all 6, see §6/§8 for the decisions. Bugs: `DetonationResult`/`DetonationForm` were an invented shape, not `harness/detonate.js`'s real output — rewrote both as a two-variant union modeled directly on the producer (`redirect_chain` is now `{url,status}[]`, matching detonate.js's actual pushes; forms use the real field names `action_origin`/`method`/`cross_domain`/`asks_password`; error results no longer require `summary`/`final_url`, matching detonate.js's error-path returns). `EvidenceEvent` previously let any lane pair with any evidence type — replaced with a discriminated union pairing lane to its valid evidence shape(s), with a compiled proof it's enforced (`contracts/events.typecheck.ts`, verified to genuinely fail without its `@ts-expect-error` guard). Rule findings: added a maintenance-process note directly in `events.ts`'s header; PR size only partly addressed (464 lines, still over ~400) — result: `tsc --noEmit --strict` clean on both files; fixture re-validated field-by-field with a runtime structural checker (not committed). Pushed, PR #12 still open
2026-08-26 · T-016 (Qodo remediation, round 2) · [O3] · Actually resolved the PR-size finding rather than documenting an exception: re-formatted `contracts/fixtures/mission-happy-path.json` (flat/repetitive objects collapsed to single lines, still valid multi-line JSON, no event or field dropped) — 190 → 91 lines, `contracts/` diff 464 → 365. No type-safety or coverage traded away. Re-ran the same validation (tsc strict + runtime structural check, 20/20 pass) — result: total PR diff (incl. PLAN.md) now under 400 lines. Pushed, PR #12 still open, awaiting Qodo re-review + 2nd approval
2026-08-26 · T-015 (Qodo pass) · [O1] · Re-checked PR #4 against current main first: findings #1/#4/#5/#6/#7 from Qodo's review (attached to stale commit 5a31de1) all trace to `detonate.js`/`detonate.test.js`/`.gitignore`, which landed in PR #3 and aren't part of PR #4's diff at all — no rebase was needed, PR #4 already sits on current main. Fixed the two real bugs/rule-violations left: `quarantine_stub` now caps `message_id` so the serialized response stays under the ~2KB MCP limit (`truncated` flag added, regression test in `stub-mcp-server.test.js`); added `harness/test/approval-gate-verification/` (throwaway test-agent JSON + `verify-approval-gate.sh` + exact cleanup instructions) so the T-015 gate-wiring proof (§4 above) is reproducible from a clean checkout — `harness/agent.json` still untouched. The 400-line PR-size finding is documented, not code-fixed — see §6. 12/12 tests pass. Pushed to PR #4, not merged — waiting on Qodo to review the new HEAD
2026-08-26 · T-015 (Qodo pass 2) · [O1] · Qodo's incremental review re-scans full file content at HEAD, not a diff against current main (see §7) — so it re-flagged `detonate.js`/`detonate.test.js`/`.gitignore` even though they're identical to main. Two of those findings turned out genuinely worth fixing on their own merits, so fixed for real this time instead of re-documenting: (1) **SSRF guard added to `detonate.js`** — the initial URL and every redirect hop now resolve the hostname and refuse loopback/RFC1918/link-local/cloud-metadata addresses (checked against the resolved IP, not just the hostname, so DNS-rebinding-style domains are caught too); `allowPrivateNetworkTargets: true` is the explicit, narrow opt-in `detonate.test.js`'s own fixture now uses, plus a new default-refusal regression test. This is a real production SSRF fix, not a Daytona workaround, and needed no Daytona credentials. (2) PR description now explicitly names `.gitignore` and its rationale (Rule 2880666). Re-verified the PR-size finding (Rule 2880655) is genuinely not reducible: lockfile is 106 packages with no duplicate/platform-variant bloat, all transitively required by the 3 declared dependencies — kept documented, not code-changed. 13/13 tests pass
2026-08-26 · T-015 (Qodo pass 3) · [O1] · Dismissed findings #4 (.gitignore undocumented) and #7 (approval-gate reproducibility) via `@qodo dismiss` replies on their review-comment threads, citing current evidence already in the PR (description names `.gitignore`; `harness/test/approval-gate-verification/` provides the reproducible setup) rather than changing working code to appease stale evidence — Qodo's own reply independently confirmed #7's evidence was stale. Final state before merge: 0 Bugs, 1 Rule violation (the documented, genuinely-unfixable PR-size finding), 0 Skill insights. Merged into main, resolving the PLAN.md conflict against PR #5 (T-003/T-004) in the same merge commit
2026-08-26 · T-016 (PLAN.md conflict resolution) · [O1] · PR #12's branch was behind main (opened before PR #4/#5 merged) — merged current main in, resolved the resulting PLAN.md conflict mechanically (this block), left `contracts/events.ts`/`events.typecheck.ts`/`fixtures/mission-happy-path.json` untouched. `tsc --noEmit --strict` re-run clean on both `.ts` files post-merge; harness `node --test` also re-run (13/13) since the merge touched shared PLAN.md history. Confirmed Qodo's PR #12 review is clean at current HEAD: 0 Bugs, 0 Rule violations, 0 Skill insights, all 6 original findings resolved. Remaining blocker is exactly one thing: the CLAUDE.md `/contracts` 2-human-approval rule — zero human approvals exist yet (`reviewDecision` empty). Not merged, implementation not altered
2026-08-25 · T-020 · [O2] `domain_intel` (`tools/imports_mcp/domain_intel.py`) — RDAP registration/registrar/abuse-contact + crt.sh cert age, wired as a second `imports-mcp` tool. Each source degrades independently to `available=false` + a note instead of raising; crt.sh results cached in-memory (never caches a transient failure, only a real answer). crt.sh was down the entire time this was built — that's a live-fire test of the fallback, not a hypothetical one. Hit and fixed a real local SSL bug along the way (see §7). 12 new tests (8 mocked-network unit tests + 3 tool-wiring contract tests + 1 more live end-to-end test alongside T-012's over the real transport) — 24/24 total pass
2026-08-26 · T-020 (Qodo pass) · [O2] Fixed 5 findings from PR #9's review: PR itself was split in two (implementation + wiring tests vs. the full unit-test suite) since the combined diff ran to 692 lines against the 400-line rule; `domain_intel()` now enforces the ~2KB MCP response budget, shortening free-text fields and setting an explicit `truncated` flag rather than ever emitting an unbounded response; the crt.sh cache now actually honours `CRTSH_CACHE_TTL_SECONDS` (was storing bare results with no timestamp, so nothing ever expired); RDAP and crt.sh parsing are now robust to technically-valid-but-malformed JSON (wrong types at any level) via `isinstance` guards plus a parsing-boundary `try/except`, so a malformed response from one source degrades independently instead of raising and starving the other source of its own lookup; the integration test's live RDAP-registrar-string assertion (`"MarkMonitor" in ...`) is gone, replaced with structural assertions that hold regardless of what RDAP actually returns. 11 new regression tests — 35/35 total pass
2026-08-26 · T-020 (Qodo pass 2) · [O2] Fixed 2 more findings: a non-list `events` value (e.g. a string) was being treated as "no registration event found" — `available: true` — instead of a malformed response, since a string is iterable and silently produced zero matches rather than raising; RDAP's own JSON-decode failure path (`raise_for_json=True`) had no regression test, nor did crt.sh's. `events` is now strictly required to be a list before any iteration; 1 existing test corrected (now asserts the malformed-shape degradation instead of the old harmless-ignore behavior) plus 2 new tests (RDAP JSON-decode-failure with crt.sh independent, crt.sh JSON-decode-failure with RDAP independent) — 37/37 total pass
2026-08-26 · T-020 (PR #9 + #14 merged) · [O1] · PR #9 (implementation + wiring/contract tests + integration-test updates) and PR #14 (the exhaustive `domain_intel` unit-test suite, split out purely to satisfy the 400-line rule — see the Qodo-pass entry above and §6) both merged: PR #9 as `1aebf464c556aed91fbc87dca94fe167f891fe52` into `tools/imports-mcp-skeleton` (first resolved a `PLAN.md`/`tools/requirements.txt` merge conflict against that moved base — kept `mcp>=2.0` from the base plus `requests`/`truststore` from this branch, no dependency lost either side), PR #14 as `5ca7fa537928899e373b2ac4da8f96a7edf5b4df` on top of that. Both PRs' remaining Qodo findings were independently verified as false positives against current code (not new fixes) and dismissed with cited line-by-line evidence: PR #9's "domain_intel response uncapped," "malformed responses abort tool," "PR exceeds 400 lines," and "crt.sh cache never expires" all traced to evidence that didn't match the already-fixed code; PR #14's "JSON failures remain untested" was factually wrong — the two JSON-decode-failure tests it claimed were missing were verified present and passing (confirmed by directly invoking the mock to prove `resp.json()` genuinely raises, not just an unused kwarg). Post-merge, confirmed directly: `domain_intel.py` and `test_domain_intel.py` both present on `tools/domain-intel`; full non-integration `tools/` suite (includes T-010/T-012's own tests too) — 59/59 pass. **T-020 is now fully complete** — implementation, wiring, and its exhaustive unit-test suite are all merged together. Not yet on `main`; `tools/domain-intel` still needs to merge up through the rest of the `tools/` stack
2026-08-25 · T-021 · [O2] `url_reputation` (`tools/imports_mcp/url_reputation.py`) — URLhaus exact-URL lookup, wired as a third `imports-mcp` tool. Confirmed exact response field names live against the real API first (`query_status`/`threat`/`tags`/`url_status`/`date_added`) before writing tests against them. "Not listed" always carries an explicit weak-signal note per trap #8, never returned as a clean bill of health. Same graceful-degradation shape as T-020 (missing key / 401 / non-200 / network error / bad JSON all become `available=false` + a note, never a raise). 11 new tests (8 mocked-network unit + 3 wiring/live) — 37/37 total pass, including two more live end-to-end calls over the real transport (URLhaus is reliably up, so unlike T-020's crt.sh case this one asserts on the actual verdict)
2026-08-26 · T-021 (Qodo pass) · [O2] · Fixed all 4 Qodo findings on PR #10: (1) `url_reputation()` now runs every return path through `_cap_response()` (Rule 2880706) — trims `tags` first, then truncates `note`/`url` as strings, until the serialized response is back under ~2KB, with a `truncated` flag and per-field `omitted` indicator. (2) A 200 with valid-but-non-object JSON (`null`, an array, a bare string) used to crash `data.get(...)` with `AttributeError` — added an `isinstance(data, dict)` check right after the JSON-decode, falling into the same graceful `available: false` path as the other malformed-response cases; 2 new tests (`null`, list). (3) The live URLhaus integration test now skips by default (`URLHAUS_AUTH_KEY` unset → skipped, not failed) instead of requiring the secret in the default suite, and its assertion is structural (shape/types) rather than pinned to URLhaus's current, mutable verdict for `https://example.com/` — deterministic behavior stays covered by the 12 mocked tests in `test_url_reputation.py`. (4) PR description now explicitly names `PLAN.md` as the root-level/cross-owner file touched and what it records. All 15 tests directly in scope (`test_url_reputation.py` + `test_server_url_reputation_contract.py`) pass; the 2 url_reputation tests in `test_server_integration.py` also pass individually/in isolation. Pushed, not merged — waiting on Qodo to re-review the new HEAD
2026-08-26 · T-021 (PR #10 merged) · [O2] · `tools/url-reputation` rebased onto the then-current `tools/domain-intel` (post PR #9+#14 merge) to pick up the T-020 Qodo-fix history it had branched before — 2 real conflicts, both in `PLAN.md` (§1 `LAST UPDATED` and §4 DONE, both additive, interleaved rather than one side dropped) plus a third file (`tools/tests/test_domain_intel.py`) that git resolved automatically by recognizing the branch's own copy as equivalent to already-applied history, so `domain-intel`'s corrected strict-events test and JSON-decode-failure coverage were kept intact, not reverted. Force-pushed with `--force-with-lease`, then PR #10's base was retargeted from the stale `tools/domain-intel-tests` to current `tools/domain-intel` (diff dropped from 940/-24/12 files to the correct 432/-6/7 files, matching T-021's actual changes only). Qodo did not produce any review or check-run for the rebased HEAD (`30b8bd7e8e336ccc7753f6920b2f2c039b51de0e`) despite the push and the retarget — confirmed repeatedly (0 check-runs, commit status `pending`, the only review on file still pinned to the pre-rebase commit `d9e213d`). Given repeated confirmation that no review would land, merging was authorized as an **explicit, one-time exception** to the "never merge before Qodo has reviewed" rule (§6) — not a standing policy change. Immediately before merging, independently re-verified: PR mergeable/clean, base `tools/domain-intel`, head still exactly `30b8bd7`, working tree clean, 15/15 T-021 tests, 24/24 T-020 regression tests, 80/80 full suite, diff scoped to T-021's own 7 files only. Merged (squash) as `766d30520393504b09184736232b2b414095365d` into `tools/domain-intel`. **T-021 is now fully complete and merged** — same as T-020, `tools/domain-intel` (now carrying both) has not yet been merged up into `main`
2026-08-26 · T-023 · [O1] · `harness/agent.json`'s `instructions` now **asks** the root agent to delegate to three named subagents in parallel (configuration via prompt, not new code — CLAUDE.md "don't rebuild the harness"), matching §10's architecture table field-for-field: INFRASTRUCTURE (`domain_intel`, `url_reputation`, `detonate`), IDENTITY (no tool — display-name vs. Reply-To/Return-Path + lookalike-domain checks on already-parsed fields), HISTORY (`correspondence_history`). Structured (non-prose) evidence output is explicitly left to T-024, not attempted here. `agent.json` still valid JSON, same top-level schema already runtime-confirmed in T-013/T-017 (only the `instructions` string value changed) — result: **written, not yet runtime-verified**, no local TrueForge instance was running this session to POST it against a live server; harness `node --test` re-run clean (13/13, unaffected — no JS changed) as a regression check only, not a substitute for that verification
2026-08-26 · T-023 (Qodo pass) · [O1] · Qodo's PR #13 review found 1 real Bug: the initial `harness/README.md`/PLAN.md wording documented the three-subagent topology as if `dynamic_sub_agents` platform-enforces it. Independently verified against TrueForge's own docs (`trueforge.dev/key-features/subagents`) — confirmed the finding: the root agent's own model decides at runtime whether/how to delegate, generating its own instructions via the built-in `create_sub_agent` tool (not from our prose), and every spawned subagent gets the **full** root tool set — no per-agent scoping exists. Fixed by rewording README.md/this file to state plainly that `instructions` is best-effort prompt guidance to the root's delegation decision, not enforced configuration — "IDENTITY: no tool call" is a request, not a boundary; "exactly three subagents" is what we're asking for, not a guarantee. The approach itself (prompt-only, no custom delegation code) stays correct — Qodo's own assessment agreed building real enforcement would duplicate native harness behavior. `agent.json`'s `instructions` text itself wasn't changed — it was already phrased as guidance to the model, not a claim about platform behavior; only the docs describing it were overclaiming
2026-08-27 · T-050 · [O3] · `cockpit/` scaffold — Vite + React + TS (stack spike + decision, see §6). `src/missionSource.ts` is the event-source abstraction: `fixtureEventSource()` plays `contracts/fixtures/mission-happy-path.json` back one `MissionEvent` at a time (an async generator, not a static array dump — matches the shape a real SSE stream will demand), and `assertMissionEvent()` runtime-validates every event (unknown `type`, or a `mission.evidence` whose `lane` doesn't pair with a real evidence shape, throws instead of rendering silently wrong — the runtime equivalent of the exact bug class Qodo's PR #12 review caught in the type itself, finding #4). `src/useMissionEvents.ts` consumes the source in order into React state; `src/MissionView.tsx` renders each event as one human-readable line built from its typed fields (not `JSON.stringify` — a `switch` over `MissionEvent["type"]`, and a nested one over `EvidenceEvent["lane"]`, both exhaustive against the contract's discriminated unions). No parallel event model invented; every type imported from `../../contracts/events`, none redeclared, per that file's own maintenance rule — result: `npm run build` (`tsc -b && vite build`, strict) clean; dev server verified serving all modules incl. the cross-folder `../../contracts/fixtures/mission-happy-path.json` import (200 via Vite's `/@fs/` resolution); direct Node execution of `missionSource.ts` confirmed all 20 fixture events consumed in exact fixture order, and separately confirmed `assertMissionEvent` genuinely rejects an unknown `type` and an invalid `lane` (not just always-passes) while accepting a valid event; `harness/node --test` 13/13 and `contracts/`'s own `tsc --noEmit --strict` re-run clean (unaffected, neither folder touched). T-051 (mission view/plan tree) is next — not attempted here
2026-08-27 · T-024 · [O1] · `harness/agent.json`'s `instructions` now tells each subagent to report back structured evidence, never prose — a small JSON object built only from the fields its own tool call(s) actually returned (INFRASTRUCTURE: one object per tool it called — `domain_intel`/`url_reputation`/`detonate`'s own result fields, omitting a key entirely for a tool it didn't call rather than fabricating one; IDENTITY: `display_name`/`reply_to`/`return_path`/`lookalike_domain`/`lookalike_of`, computed from the parsed message, no tool call; HISTORY: `correspondence_history`'s own result fields) — plus an explicit "never a field another subagent owns" line addressing T-024's other half (narrow remits, no duplicated evidence). The lead, not the subagents, turns that evidence into the final plain-English verdict. Same prompt-only approach as T-023 (configuration, not code — `dynamic_sub_agents` still has no schema-enforcement mechanism to build against). **Found and deliberately did not use** `contracts/events.ts`'s `DomainIntel`/`UrlReputation` types as the target shape: neither matches the real tool output (`domain_intel.py` nests results under `rdap`/`cert`; `url_reputation.py` has `available`/`threat`/`url_status`/`date_added`/`truncated` fields the contract omits entirely) — a real, pre-existing contract/producer drift, logged in §8 rather than fixed here since `/contracts` needs 2 approvals and is out of this task's scope. `agent.json` still valid JSON, same schema as T-013/T-017/T-023 (only `instructions` changed) — result: **written, not yet runtime-verified**, same constraint as T-023 (no local TrueForge instance this session); `node --test` re-run clean (13/13, unaffected — no JS changed), regression check only
2026-08-27 · T-024 (Qodo pass) · [O1] · Fixed 2 real bugs Qodo's PR #17 review found, both verified directly against `contracts/events.ts` and `cockpit/src/missionSource.ts`'s runtime validator before fixing, not just accepted on Qodo's say-so: (1) IDENTITY's field list omitted the required `from_address` and included a `return_path` the contract doesn't have — `IdentityEvidence` (and `isIdentityEvidence`'s runtime check) requires exactly `from_address`/`display_name`/`reply_to`/`lookalike_domain`/`lookalike_of`; a missing `from_address` genuinely fails Cockpit's validator (`fail(index, ...)`), confirmed by reading the check, not assumed. Fixed by adding `from_address` and keeping `return_path` as a bonus field alongside it (validator doesn't reject extras, only checks the required ones are present and typed). (2) The "never a field another subagent owns" line was genuinely ambiguous: `domain_intel` and `correspondence_history` both have their own `domain` field, and `CorrespondenceHistory`'s contract requires HISTORY's `domain` — a literal reading of "ownership" could tell the model to drop HISTORY's required `domain` just because a different tool's output happens to share that key name. Reworded to make "own remit" explicitly about judgment (only IDENTITY decides lookalike-domain, only INFRASTRUCTURE calls its three tools), not field names — a shared key name across two tools' own outputs is never a reason to drop or rename either one. `harness/README.md` updated to match both fixes. `agent.json` re-validated as JSON; `node --test` re-run clean (13/13, unaffected). Pushed to PR #17, not merged — waiting on Qodo to re-review the new HEAD
2026-08-27 · T-024 (PR #17 merged) · [O1] · Qodo re-reviewed the fixed HEAD clean (0 Bugs/0 Rule violations/0 Skill insights, both prior findings resolved) — merged into `main` as `43dda6f7c90a3ca9f9ae6f50ed5438a6f4e6c0df`. Confirmed directly (not assumed): `git show origin/main:harness/agent.json` contains the structured-evidence instructions including `from_address`. **T-024 is now fully complete and on `main`** — same runtime-verification gap as T-023 still applies (§5): written and merged, but no local TrueForge instance has observed a real turn's actual delegation/reporting behavior yet
2026-08-25 · T-010 · [O2] Normaliser (`tools/imports_mcp/normaliser.py`) — RFC822 parse via stdlib `email`, URLs via `lxml.html` (href + anchor text, plain-text bare-URL fallback), attachment SHA256, raw Authentication-Results/Received chains passed through unverified. Smoke-tested against a synthetic phish + a synthetic legit mail, both correct
2026-08-25 · T-011 · [O2] 3 fixtures in `tools/fixtures/` — credential phish (SPF/DKIM/DMARC fail, raw-IP href vs polished anchor text), invoice fraud/BEC (homograph lookalike domain `universaI-imports.co` where auth actually PASSES, reply-to redirected to a third domain, PDF attachment), and one legitimate mail that must not trip anything. All hand-written `Authentication-Results`/`Received` headers per trap #11. Ran the T-010 normaliser against all three — every signal (auth fail, href/anchor mismatch, reply-to mismatch, attachment hash) came through correctly
2026-08-25 · T-012 · [O2] `imports-mcp` skeleton (`tools/imports_mcp/server.py`) — one tool, `parse_message(fixture)`, wired to the T-010 normaliser against the T-011 fixtures. Streamable HTTP per T-004, whitelisted fixture lookup (rejects unknown names and path traversal via `ToolError`, not a bare exception). 11 tests: 9 contract-level (direct function calls, no server) + 2 real end-to-end over the actual HTTP transport with the official MCP client — all pass
2026-08-26 · T-010 (Qodo pass) · [O2] Fixed 3 real bugs Qodo's PR #6 review found: `_extract_urls` now pulls from **both** multipart alternatives instead of returning as soon as HTML existed (was silently dropping plain-text-only links, and returned nothing at all on a failed HTML parse), deduplicated by href with HTML's anchor text preferred; bare-URL matches now strip trailing sentence punctuation while preserving a URL that legitimately ends in a balanced paren (e.g. a Wikipedia article path); HTML extraction now keeps only absolute `http(s)` hrefs, dropping `mailto:`/`javascript:`/fragments/relative paths. 17 new regression tests, all passing — result: `urls` now matches the module's own "every URL" contract
2026-08-26 · T-012 (Qodo pass) · [O2] · Fixed all 5 Qodo findings on PR #8: (1) `requirements.txt` pinned `mcp>=1.0`, but `server.py` imports `MCPServer` — the v2-only rename of v1's `FastMCP` (independently confirmed against the SDK's own v2.0.0 release notes: "v1.x is in maintenance mode," `MCPServer` doesn't exist there) — bumped to `mcp>=2.0`, confirmed by installing fresh (`mcp-2.1.1` resolved) and re-running every test. (2) `parse_message` returned the normaliser's dict uncapped — added `_cap_response()` (Rule 2880706): trims `received_chain`/`urls`/`authentication_results` until the serialized response is back under ~2KB, setting `truncated: true` plus a per-field `omitted` count as the continuation indicator; 2 new regression tests (small result untouched, oversized `received_chain` actually gets capped under the byte limit). (3) `pytest` from the repo root couldn't import `imports_mcp` (no `tools/tests/__init__.py`, so pytest's default import-mode inserted `tools/tests/` onto `sys.path` instead of `tools/`) — added the empty `__init__.py`; verified with both `pytest tools/tests/` and bare `pytest` run from repo root. (4) `_resolve_fixture()` accepted any regular file in `FIXTURES_DIR`, not just the advertised `.eml` whitelist — added a `candidate.suffix != ".eml"` check alongside the existing containment/is-file checks; new regression test drops a real `.txt` file into `FIXTURES_DIR` and confirms it's still rejected. (5) The integration test hardcoded port 8951 and treated any successful TCP connect as "server ready" — now binds to port 0 to get a free ephemeral port per run, and the readiness poll checks `proc.poll()` first so a dead child fails fast with its captured output instead of the full timeout. All fixes installed fresh (`mcp-2.1.1`, `pytest-9.1.1`) and verified: `pytest` bare from repo root — **14/14 pass** (the original 11 — 9 contract + 2 integration, integration now on an ephemeral port — plus 3 new regression tests: 2 for the response cap, 1 for the `.eml` whitelist). Pushed, not merged — waiting on Qodo to re-review the new HEAD
2026-08-26 · T-012 (Qodo pass 2) · [O2] · Qodo's re-review still flagged "root pytest cannot import package" as open even after `tools/tests/__init__.py` landed — verified directly (clean env, no `PYTHONPATH`) that `imports_mcp` already resolves fine from repo root today: pytest's own default import-mode inserts `tools/` onto `sys.path` because `tools/tests/__init__.py` exists but `tools/__init__.py` doesn't. That's real, but implicit and undocumented — exactly what Qodo's evidence named ("no pyproject.toml/pytest.ini/tox.ini/setup file exposes `tools/`"). Added a repo-root `pytest.ini` (`pythonpath = tools`, pytest's native option, no new dependency) to make the exposure explicit rather than accidental. Root placement isn't a style choice — pytest's ini-file discovery only searches upward from the invoked path, so confirmed empirically that a `tools/pytest.ini` is silently skipped by a bare `pytest` run from the repo root while a root-level one is picked up by both bare `pytest` and `pytest tools/` (`configfile: pytest.ini` now shown either way) — result: 14/14 pass, all 4 previously-resolved findings reconfirmed intact in code
2026-08-27 · Integration (Qodo pass on PR #19) · [O2] · Qodo's first review of the integration PR found 7 Bugs + 4 Rule violations. Fixed 6, deferred 3 with recorded reasoning, all within `tools/` only (no `contracts/`/`cockpit/`/`harness/` touched). **Fixed:** (1) `parse_message`'s real output didn't match `ParsedMessage`/Cockpit's runtime validator at all — `from`/`reply_to`/`return_path` were nested `{display_name, address}` objects instead of scalar strings, `authentication_results` was a list instead of a string, and `message_id`/top-level `display_name` were missing entirely; unlike `DomainIntel`/`UrlReputation` (below), nothing merged elsewhere depends on this shape yet, so fixed the producer (`normaliser.py`) to match the existing contract instead of deferring — `from`/`reply_to`/`return_path` now plain address strings, `display_name` taken from the From header only, `authentication_results` is multiple headers joined with `\n`, `message_id` from the `Message-ID` header. `server.py`'s `_cap_response` simplified to match (the old dict-shaped address-trimming branch is dead code now — removed, not left unreachable). (2) `_extract_urls` promised "every URL from every body alternative" but `get_body(preferencelist=...)` only returns one best-match part per content type, silently skipping a secondary/nested multipart/alternative group (e.g. a forwarded message embedded as its own part) — rewritten to `msg.walk()` every leaf part, still collecting HTML before plain-text so the existing "HTML anchor wins the dedup" preference holds regardless of document order. (3+8) `url_reputation`'s `_cap_response` only ever trimmed `tags`/`note`/`url`, leaving `threat`/`url_status`/`date_added` unbounded — a oversized untrimmed field could keep the response over the 2KB cap forever, and worse, `note`/`url`'s own shrink step (`max(1, len(value)-200)`) plateaued at one leftover character instead of reaching `""`, so the loop never terminated once a field got that short while the response was still oversized. Rewritten with a `_shrink_string` that strictly shrinks toward `""` (mirrors `server.py`'s own pattern) applied to all five upstream scalar fields, with a final fits-check after each. (4) `domain_intel`'s `_cap_response_size` measured the payload *before* adding `truncated: false`, so a result exactly at the boundary could return a few bytes over the declared 2KB cap while still claiming untruncated — now builds the full flagged payload first, checks that, and re-verifies after truncating (with a second, more aggressive truncation pass if the first round isn't enough). (6) `url_reputation`'s listed-result path copied URLhaus's `tags` field without checking it was a list of strings — a malformed-but-valid-JSON response (e.g. `tags: 123`) would crash `_cap_response`'s `list(tags)` call outright; now validated and degraded to `available: false` before ever reaching a caller, same "valid JSON, wrong shape" guard `domain_intel` already applies to RDAP's `events`. (7) `test_server_integration.py`'s live `domain_intel` RDAP/crt.sh call had no gate (unlike the URLhaus test, which already skips without a key) despite PLAN.md's own §7 recording that this exact call reproducibly times out a later test in the same shared server subprocess — gated behind `RUN_LIVE_DOMAIN_INTEL_TESTS=1`, off by default, same pattern as the URLhaus gate. (10) `domain_intel`'s server wrapper only checked for empty/whitespace before interpolating the caller's string into the RDAP URL path — a domain containing `/`, `?`, `#` would query a different path/query than intended; added an RFC-1035-shaped hostname regex + 253-char length check at the tool boundary, before `_domain_intel` ever runs. 25 new regression tests across `test_normaliser.py`/`test_domain_intel.py`/`test_url_reputation.py`/`test_server_domain_intel_contract.py`/`test_server_contract.py` (existing `_cap_response`/contract tests in `test_server_contract.py` also updated for the new flat shape) — 101/101 pass, 1 correctly skipped (the newly-gated live test; confirmed it still passes when explicitly opted in). `harness` 13/13, `contracts`/`cockpit` unaffected (untouched). **Deferred, not fixed here (finding #2):** `domain_intel`'s real nested `rdap`/`cert` output still doesn't match `contracts/events.ts`'s flat `DomainIntel` — Qodo independently rediscovered exactly the drift already logged in §5/§8 (2026-08-27). Left as-is on purpose: `harness/agent.json`'s merged T-023/T-024 instructions already deliberately target `domain_intel`'s *current* real field names verbatim (§4, T-024 entry) — flattening the tool now would silently break that already-reviewed prompt guidance, and the actual fix (`contracts/events.ts`) needs 2 approvals and is out of this PR's scope regardless. Qodo's own high-level assessment on this PR agreed: "that focused follow-up should land before live domain-intelligence events reach Cockpit" — separate `/contracts` PR, not this one. **Declined (findings #9, #11):** `.gitignore`'s tools-artifact entries weren't named in the original PR description — fixed by updating the PR description directly (cheap, no code change needed). The 400-line-PR rule violation (finding #11) stands as previously documented (§6, 2026-08-27 integration entry) — an integration merge carrying already-individually-Qodo-reviewed commit history isn't the same class of risk as one large unreviewed diff; no code change, position restated on the PR
2026-08-27 · T-020 (`DomainIntel` contract resolved, PR #19 commit `3f169b3`) · [O2] · **Backfilled entry — this commit shipped without one.** Resolved the `DomainIntel` contract mismatch left open above: `domain_intel()` now mirrors `registration_date`/`registrar`/`abuse_contact`/`cert_issued_at` at the top level (a new `_resync_flat_mirrors()` keeps them equal to the `rdap`/`cert` sections that feed them, including after truncation), satisfying `contracts/events.ts`'s existing flat `DomainIntel` type and `cockpit/src/missionSource.ts`'s `isDomainIntel` runtime check. The nested `rdap`/`cert` sections are kept exactly as they were — `harness/agent.json`'s T-023/T-024 instructions and this module's own other tests still depend on them by name — so this is additive, not a breaking reshape, and needed no `/contracts` PR or 2-approval gate. 4 new regression tests, including one that asserts the mirrors actually equal their nested sources (not just that the keys exist), plus one for the null-RDAP case. Also split `test_server_integration.py`'s two opt-in live tests into their own `test_server_integration_live.py` with an isolated server subprocess each (via new `tools/tests/_server_fixtures.py`), so PLAN.md §7's documented shared-subprocess stall risk can no longer reach the always-on deterministic tests in the same file even when a live test is explicitly opted into. 103 passed, 2 skipped in the environment this was built in (no `URLHAUS_AUTH_KEY` configured there); re-verified independently while writing this backfilled entry — 104 passed, 1 skipped here (this machine's `.env` has the key, so `url_reputation`'s live test also runs). `contracts/`, `cockpit/`, `harness/`, `PLAN.md` all confirmed untouched by this commit — only `tools/` changed. Closes the §5 blocked row and the §8 suggestion below

2026-08-28 · T-034 · [O1] · **Four sequential licence gates configured in `harness/agent.json`.** `mcp_servers` was `[]`, so this added the `imports-mcp` connector itself (referenced by name; the URL lives in TrueForge's own connector settings, not the manifest) and set `require_approval_for_tools` to `quarantine`/`notify_impersonated`/`create_block_rule`/`file_abuse_report` in §10's order. Literal names deliberately **replace** the field's `["@write", "@destructive"]` default, so exactly those four gate and the read-only tools stay ungated regardless of annotation; `enable_tools` left at `["@all"]`. Configuration only — **T-030–T-033 are still unbuilt and were not touched.** The open question going in was whether TrueForge rejects gate names for tools that don't exist; it does not. Verified live (WSL2, real TrueForge, real `imports-mcp` registered): connector registered `201`, `GET /api/v1/mcp-servers/{name}/tools` returned exactly `parse_message`/`domain_intel`/`url_reputation` — confirming all four gate targets genuinely absent — and an agent carrying this exact gate list was **not** rejected for naming them, reaching the expected `422 provider not configured`. Two deliberate controls *were* rejected `400` with precise `manifest.mcp_servers[0]...` schema errors (empty gate string; missing `name`), proving manifest validation really runs rather than the 422 masking a skipped check; the live OpenAPI schema agrees (`MCPServerApprovalToolSelector` accepts any non-empty string). Post-fix, the **real** `harness/agent.json` was POSTed live and reached the same expected 422 — full manifest schema-accepted, same result as T-013/T-017. **Qodo caught 1 real Bug** (PR #26): `instructions` still ended "propose the action and wait, never assume consent", which tells the model to stop conversationally *before* emitting the tool call — but CLAUDE.md and §10 both specify the harness pauses *on the gated call*, so that wording would have prevented the gates this task just configured from ever firing. Correct when written (T-013, `mcp_servers` empty, prompt restraint was the only safety); T-034 is what made it harmful. Reworded to call the gated tool directly and let the harness pause it, plus a denial is final and must not be routed around via another tool. New `harness/agent-config.test.js` pins the four names, their order, and that no read-only tool is gated — a gate-name typo is schema-valid and would silently ungate an irreversible action, so it can't be left to review alone; mutation-checked that it actually fails on `"quarentine"`. Deliberately no prompt-phrasing test (prompt text is legitimately reworded; a grep-for-absence guard would fail on innocent edits while proving nothing about behavior). `harness` 18/18, `tools` 103 passed/2 skipped. Qodo re-reviewed the exact final HEAD `3fccba8` clean (0/0/0, the Bug Resolved) — merged into `main` as `7e4fade2ac41e7d47120b1872ff805f3ad8d3462` — result: **configuration proven accepted; no `tool.approval_required` event has been observed and cannot be until T-030–T-033 exist *and* a model provider key is configured (§5).** Separately found, not fixed here: `harness/test/approval-gate-verification/verify-approval-gate.sh` still uses T-015-era unprefixed routes that now 404 (they moved under `/api/v1/`, and registration bodies are wrapped in `{"manifest": {...}}`) — stale independently of T-034

2026-08-28 · T-031 · [O1, taking over from §11's O2 listing] · **`notify_impersonated` — the first of the four T-034 gate-target tools to actually exist** (`tools/imports_mcp/notify_impersonated.py`), over stdlib `smtplib`, Range-only by default (`localhost:1025`, T-060's Mailpit — CLAUDE.md trap #6 applies to this tool the same as `file_abuse_report`). Cross-folder takeover, disclosed in the PR: O2 was occupied with the blocked T-022, and T-030–T-033 were verified genuinely unstarted (no open PR, no named branch, zero hits grepping every remote ref for any of the four function names) before picking this one specifically — it's the one that unblocks the most (T-034's gates are configured but none could fire with zero gate-target tools existing). PR #29 opened with 4 Qodo findings; 3 were real (blank `SMTP_HOST` from a copied `.env.example` silently no-op'd every send instead of falling back to Mailpit; `SMTP_PORT=0` passed straight to `smtplib`, which treats a falsy port as its own default of 25 — a real outbound MX port, not the Range; `smtp_host` being env-controlled and unbounded wasn't in the response-cap's trim list). Fixed in a follow-up commit (`6f64ac6`) — `_smtp_target()` now treats blank/whitespace host and any port outside 1–65535 as unset, not just a missing var or non-numeric string; `_failed()` takes the already-resolved host instead of re-reading the (possibly blank) env var. Qodo's automated re-scan marked all 3 **✓ Resolved**. The 4th finding (587 lines, over the 400-line rule) was a documented exception, not a split — reasoning inline in the PR: this tool has an irreversible outward side effect, and its Range-only safety properties (default-target assertion, never-port-25 assertion, header-injection rejection, response cap) exist only as tests in this same diff; splitting them into a separate PR would put the mail-sending code in front of a reviewer without its safety evidence alongside it — the same failure mode §7 already recorded for T-020's split. Precedent: PR #4 (1,549 lines), PR #19 (2,623 lines), both in §6/§8. Dismissed via `@qodo dismiss` citing that reasoning (same mechanism as the 2026-08-26 PR #8 precedent) rather than waiting on a re-scan that can't detect a description-only fix. Merged into `main` as `6550c20051de7c7899bb9b5a0657231f6984da73`. Verified before writing this entry: `tools` 146 passed/3 skipped, `harness` 18/18 unaffected, no conflicts merging against current `main` (confirmed by a direct local test-merge before the squash-merge, not assumed from the GitHub API's clean status alone). Removed from §11 (was still listed there under its original `[O2]` line). **Explicitly not claimed:** no `tool.approval_required` event has been observed — T-015's own blocker is unchanged by this (§5 names exactly one: no model provider key configured; T-015's live-fire test uses its own separate `quarantine_stub`, unrelated to which of the four real product gate-target tools exist); a model provider key is still needed for either. T-030/T-032/T-033 remain O2-owned and untouched — this was a one-task takeover, not a claim on the rest of T-034's gate-target tools

2026-08-28 · T-051 · [O3] · **Mission view is now a plan tree, replacing T-050's flat event log.** New `cockpit/src/missionPlan.ts` — pure, framework-free, no React import — turns the `MissionEvent[]` stream into the six §10 architecture stages (message received → evidence gathering → detonation → verdict → licence gates → complete), each rendered from event 0 as `pending`, `active` once its own events start arriving, `done` once the stream moves past it (or the whole mission completes). Evidence's three lanes and the licence gates' four slots are always shown — not invented per-mission, they come directly from §10's diagram and the contract's own `gate_index: 1 | 2 | 3 | 4` — populated only by whatever events actually arrived for them, so a live TrueForge stream reads the same way the fixture does. A denied gate gets its own `denied` status (not lumped into `done`) since §17 calls the denial out specifically as proof the gate is real. `MissionView.tsx` rewritten to render this tree (nested `<ol>`, one level per stage/child) instead of T-050's flat list; `describeEvent`/`describeEvidence` moved into `missionPlan.ts` unchanged and reused as each node's one-line detail text — no new event schema, no fields invented beyond what `contracts/events.ts` already defines. `index.css` replaces the old `.mission-view`/`.mission-event*` rules with `.plan-tree`/`.plan-node*` (status-colored left border + dot: grey/dim pending, orange active, green done, crimson denied). `App.tsx`, `missionSource.ts`, `useMissionEvents.ts` untouched — the fixture consumer T-050 built is reused exactly as instructed, nothing re-invented. **Verified, not assumed:** `npm run build` (`tsc -b` strict + `vite build`) clean; a throwaway `npx tsx` script (not committed) ran `buildMissionPlan` against the real fixture at 10 checkpoints (0, 1, 3, 5, 6, 7, 9, 12, 19, 20 events) confirming every stage/lane/gate transitions pending→active→done in the right order and with the right detail text at each point, plus a separate run with gate 3's resolution forced to `deny` confirming it renders `denied`/"DENIED" with no execution and gate 4 still `pending`; dev server smoke-tested — `index.html`, `main.tsx`, `MissionView.tsx`, `missionPlan.ts`, and the cross-folder fixture import all serve `200`. **Not verified:** actual in-browser rendering — no browser automation tool is available in this session, so the DOM/CSS output was never visually confirmed, only that the modules resolve and the underlying tree logic is correct; flagging this explicitly per CLAUDE.md rather than claiming a check that wasn't done. T-052/T-053/T-054 (rich per-lane/detonation/verdict panels) deliberately not started — out of scope for this task, the tree's per-node `detail` text is the placeholder T-052 will replace with real lane UI, same relationship T-050's flat line had to this task

2026-08-28 · T-051 (final — merged) · [O3] · **T-051 is now fully on `main`, in two PRs, both Qodo-reviewed clean.** The original single PR (464+ changed lines, over the 400-line rule — §7) was split: **PR #34** (`cockpit/src/missionPlan.ts` alone, the pure plan-tree logic, 282 lines) merged first as `e5b06fa966a1cca1e6ce2b7da0fb70588d0cdb8c`; **PR #32** (`MissionView.tsx`/`index.css`/this file, rebased onto the new `main` once #34 landed, dropping its diff to 200 lines) merged second as `7db05f75aad473769cdfb35c77d42d53b13869af`. Three real Qodo Bugs fixed across the two review rounds, all verified against the code before fixing, not accepted on say-so: (1) gate state was built only from `approval_required`, so a resumed/reconnected stream (T-056) delivering `approval_resolved`/`action_executed` without this client ever seeing the matching request silently dropped that gate to `pending` — fixed by building state from whichever of the three independently-valid events arrived, using `action_executed`'s own `action` field as a fallback label source; (2) execution was detected via `if (resultSummary)`, so a real `action_executed` with `result_summary: ""` stayed `active` instead of `done` — fixed with a separate `executed: boolean`; (3) the plan node's only status indicator was `aria-hidden`, so pending/active/done was conveyed by color/opacity alone — fixed with a `.sr-only` status label. A 4th finding, found on PR #34's own review, was real too: `approval_resolved`'s optional `reason` (validated contract data) was captured but never surfaced, reducing a denial to generic "DENIED" text — fixed, now appended to the gate's detail whenever present. **One Qodo finding was stale, not fixed:** PR #32's final review still cited the pre-rebase PR size (464 lines) for the size-rule finding specifically, even though every other finding in the same pass correctly re-scanned the rebased HEAD — corrected in a PR comment citing GitHub's own `additions`/`deletions` count (119+81=200) rather than waiting on an in-thread dismiss mechanism that had no thread to attach to this time (§7 has the general lesson). `npm run build` clean on every pushed commit; full `buildMissionPlan` logic re-verified against the real fixture after each fix (all checkpoints, denial path, missing-approval_required path, empty-summary path, reason-present path) via throwaway `npx tsx` scripts, none committed. In-browser rendering still not independently verified — no browser tool available this session, same caveat as the entry above

2026-08-28 · T-052 · [O3] · **Evidence lanes — the three-column panel that makes the parallel subagents visible (§17, 0:20-0:45), merged into `main` as `449d9e12e075f111ba52e61278a58c4c57f1ea05` (PR #36).** New `cockpit/src/EvidenceLanes.tsx` renders full typed fields per lane (domain age/registrar/cert/abuse contact, URLhaus listing/tags, lookalike info, prior-contact stats), not T-051's collapsed one-line join — every field in `contracts/events.ts`'s `DomainIntel`/`UrlReputation`/`IdentityEvidence`/`CorrespondenceHistory` is shown, `null` skipped per §12 rather than rendered blank. `missionPlan.ts`'s inline evidence-lane derivation (previously duplicated only inside `buildMissionPlan`) is now a shared exported `buildEvidenceLanes()`, which `buildMissionPlan` calls internally — the plan tree and this new panel can't disagree about a lane's state, because there's only one place that computes it now. **Qodo caught 1 real Bug** (PR #36): the lane-status expression checked `items.length === 0` before stage advancement/`missionDone`, so a lane that never reports anything (a subagent that genuinely finds nothing, or a resumed stream missing its events) stayed `pending`/"Waiting…" forever, even after the mission finished — disagreeing with the parent tree node, which correctly goes `done` at completion regardless of lane content. Fixed by checking stage advancement first: empty only means "hasn't reported yet" while evidence is still the current stage; once the stream moves past it, an empty lane is `done` with "Nothing reported" text, not stuck. **This was actually a pre-existing bug in T-051's own original inline version of the same logic (PR #32/#34) — never caught then because the fixture always populates all three lanes; T-052's refactor made it visible by making the logic a shared, independently-reviewed function** (§7 has the general lesson). `npm run build` clean on both pushed commits. Verified: the `missionPlan.ts` refactor confirmed byte-identical to pre-refactor tree output via the same checkpoints T-051 used; `buildEvidenceLanes` checked directly against the fixture (right item counts/status per lane); `react-dom/server`'s `renderToStaticMarkup` exercised all 4 evidence shapes with no throw, real field values confirmed present in the output; after the fix, a stream with identity's evidence event dropped entirely and run to completion now correctly shows identity `done`/empty instead of stuck `pending`, with the tree agreeing, and a genuinely still-in-progress empty lane confirmed to still show `pending` (the fix didn't just flip the check backwards). Dev server smoke-tested. In-browser visual rendering still not independently verified — no browser tool available this session, same caveat as T-051

2026-08-28 · T-053 · [O3] · **Detonation panel — redirect chain, forms, summary, merged into `main` as `94f90ea60f2418acddd9c75aaf1e0e3bd17e7104` (PR #41).** New `cockpit/src/DetonationPanel.tsx` renders the full `DetonationResult`: hop-by-hop redirect chain with the final URL highlighted, every form's action/method/cross-domain/asks-password fields, and the summary — the exact "asks for a password and posts it to a different domain" combination (§17, 0:45-1:25) is called out with its own warning style, not just plain text. Both `DetonationResult` branches handled (error, success) and `DetonationForm`'s `action_invalid` case. **The screenshot half is deliberately not faked:** `screenshot_id` is a documented stretch goal `detonate.js` never sets (contracts/events.ts's own comment), and no image-serving mechanism exists anywhere in this repo — no endpoint, no static-path convention, nothing decided in PLAN.md. Rather than guess at an API that doesn't exist (CLAUDE.md), renders an honest "No screenshot — text-mode detonation (T-001/T-035, §5)" state when absent (currently always, including in the fixture), and just the identifier — not a fabricated `<img src>` — if `screenshot_id` is ever present. **Qodo caught 1 real Bug:** both `DetonationPanel.tsx` and `missionPlan.ts`'s own `detonationEvent` lookup used `events.find()`, which returns the *first* matching event — so a retried/re-emitted detonation would leave the panel and tree permanently stuck on the stale first result, never showing anything more current. Inconsistent with how gate/lane state already track the stream's current state everywhere else in this app. Fixed in both places with `findLast()`. **A related, same-class bug was found but deliberately left unfixed, out of this task's scope:** `missionPlan.ts`'s `messageEvent`/`verdictEvent` lookups use the identical `events.find()` pattern on different event types — not touched here since T-053 only concerns detonation, logged below for whoever picks it up. `npm run build` clean on both pushed commits. Verified: `react-dom/server`'s `renderToStaticMarkup` checked the pending state, the real fixture's success case (every field confirmed present in the markup, including the dangerous-form warning and the honest no-screenshot state), a synthetic error branch, the `action_invalid` form case, an empty-forms case, a `screenshot_id`-present case confirming no `<img src>` gets guessed, and — after the fix — a synthetic two-event stream (a failed first attempt then a successful retry) confirming both the panel and the tree now show the *latest* result with the stale one gone from the markup entirely, plus the normal single-detonation case confirmed unaffected. Dev server smoke-tested. In-browser visual rendering still not independently verified — no browser tool available this session, same caveat as T-051/T-052

2026-08-28 · T-054 · [O3] · **Verdict panel — the demo's plain-language judgment moment (§17, 2:40-3:00: "Plain English, spoken: 'This is fake...'" — 1:25-1:50 is the evidence recap, a real citation error caught by Qodo on PR #44, corrected here), merged into `main` as `5431e70a797960fdb275dbec22b22e059933d1c3` (PR #43).** New `cockpit/src/VerdictPanel.tsx` renders the verdict label (Malicious/Suspicious/Legitimate) and summary, color-coded and sized as the most visually prominent panel — the sentence-count/plain-language constraint on `summary` itself belongs to whoever writes it (harness prompt, T-024), this panel only renders it clearly. Applied the T-053 `findLast()` lesson (§7) proactively from the start, and fixed `missionPlan.ts`'s own `verdictEvent` lookup to `findLast()` too, since this task directly touches verdict semantics (§7 had flagged this exact spot). **Qodo caught 2 real accessibility Bugs:** (1) the suspicious verdict's label used plain CSS `orange` as text color — measured ~1.97:1 contrast against white, failing WCAG's 3:1 minimum even for this large bold label; replaced with `#b45309` (~5.0:1 on white, ~4.2:1 on black, computed directly), clearing the bar in both halves of this page's `color-scheme: light dark`. (2) the verdict swaps from "Waiting…" to the arriving judgment with no live region, so screen-reader users get no notification the mission's most critical result just appeared; wrapped the swapping content in a stable `role="status"` element (implies `aria-live="polite"` + `aria-atomic="true"`) so the transition fires as one announcement. Both fixed, re-reviewed clean (0/0/0). `npm run build` clean on both pushed commits. Verified: `react-dom/server`'s `renderToStaticMarkup` checked the pending state, the real fixture's malicious verdict (label + summary present), all three verdict labels each with their own CSS class, a synthetic two-verdict stream confirming both the panel and the tree's verdict node show the *latest* verdict with the stale one gone from the markup, and post-fix confirmed `role="status"` present in the markup. Dev server smoke-tested. In-browser visual rendering still not independently verified — no browser tool available this session, same caveat as T-051/T-052/T-053

2026-08-28 · T-055 (investigated, blocked) · [O3] · **Not implemented — genuinely blocked, not disproven like T-025.** Investigated read-only: T-055's data dependency (persisted past missions, cross-mission campaign linkage) doesn't exist anywhere in this codebase — T-040 (O1, session persistence) is still unpicked; TrueForge's only confirmed live API (T-002) covers single-session/turn mechanics, not a session-history listing; §10's "write campaign dossier" names no tool and no storage format; §17's demo script has no dossier beat at all. CLAUDE.md explicitly lists session persistence under "don't rebuild what the harness already does," so no localStorage/database workaround was built and no undocumented API was guessed at. Full reasoning in §5. Moved back to §11 (marked blocked) rather than left occupying a NEXT UP slot indefinitely; will become buildable once T-040 or a confirmed TrueForge history API exists

2026-08-29 · T-056 · [O3] · **Resumable-stream reconnect handling, merged into `main` as `4c83f5caa919711608209a38efccad3c9201609c` (PR #47).** New `cockpit/src/resumableStream.ts` implements and tests the confirmed reconnect mechanic (T-002/§6): `GET /turns/{id}/subscribe?after_sequence_number=N` resumes a dropped stream from the last sequence number seen, not from the start. Transport-agnostic — takes a pluggable "open a stream starting at sequence N" function, so it's genuinely testable without a live TrueForge connection. **Deliberately scoped to the reconnect mechanic alone, not the full live `trueForgeEventSource`:** building that also needs a translation layer from TrueForge's raw turn-stream events into this app's `mission.*` events, and nothing in `contracts/`or PLAN.md has ever defined that mapping (T-002 only confirmed generic session/turn/tool-approval mechanics) — inventing that mapping would be the same undocumented-API-guessing trap T-055 was blocked on, so it's logged in §8 below, not solved here. Also deliberately not wired into `App.tsx`: the current fixture source never drops, so wiring this in would fake a "reconnecting" UI state with no real backend behind it. Qodo reviewed clean (0/0/0), no findings. `npm run build` clean. Verified via a throwaway `npx tsx` script (not committed), 5 scenarios: happy path (no drop); a mid-stream drop resuming from the correct offset with no lost/duplicated events; a resume-boundary duplicate (server re-sends its last item) correctly deduplicated; exceeding `maxReconnectAttempts` throws the real error with status `error`; exponential backoff delay genuinely measured growing between attempts. **Cross-owner discovery while selecting the next task:** T-036 (LICENCE REQUIRED panel — renders TrueForge's approval request in our styling, Allow/Deny per gate) has been sitting unpicked in §11's Slice 3 list this entire time, outside the "from join day" Cockpit list every previous O3 session scanned. **Only its presentation half is unblocked** — `contracts/fixtures/mission-happy-path.json` already carries real `mission.approval_required`/`mission.approval_resolved` events for all 4 gates, so the request panel + Allow/Deny UI is genuinely buildable now; the live-submission half needs the same undecided TrueForge→`mission.*` translation layer just logged above, so it's blocked, not done, and T-036's own §2 row says so explicitly (self-correction after Qodo caught this PR's first draft overclaiming "fully unblocked" — inconsistent with this very entry). Squarely in §17's demo script (1:50-2:20) for the presentation half. Picked next, ahead of T-043/T-044 which come later in the backlog order

2026-08-29 · T-033 · [O1, taking over from §11's O2 listing] · **`file_abuse_report` — the second of the four T-034 gate-target tools to exist.** Same disclosed cross-owner takeover as T-031: O2 was occupied with T-026 (PRs #37/#38) and T-022 (itself blocked, §5), and T-033 was verified unstarted — no branch, no PR, and `git grep "def file_abuse_report"` across *every* remote ref returned nothing. Signature `file_abuse_report(domain, evidence)` comes from §10's tool table and `contracts/fixtures/mission-happy-path.json` verbatim, so **`contracts/` needed no change** and its 2-approval rule was never engaged. Takes a *domain*, not an address: the abuse mailbox is resolved from RDAP via the merged `domain_intel` (T-020), whose flat top-level `abuse_contact` mirror (added in PR #19) this consumes. **Shared SMTP machinery extracted, not copied** — new `tools/imports_mcp/_smtp.py` now holds target resolution, address validation and response capping; `notify_impersonated` delegates to it with its public surface unchanged, and T-031's 43 tests passing untouched is the regression proof (run immediately after extraction, before any T-033 code). Extraction rather than duplication was deliberate: the port-0 guard (smtplib maps port 0 to **25**, the real outbound MX port) came from Qodo's PR #29 review, and two copies of a safety guard drift. **Safety is most of this task**, because unlike T-031's fictional `.example` recipient this tool mails *a real registrar's real abuse mailbox* straight from RDAP — exactly CLAUDE.md trap #6. So on top of the Range-by-default target it **refuses outright** unless the resolved SMTP host is loopback, unless `ALLOW_EXTERNAL_SMTP=1` is deliberately set (documented in `.env.example`; anything but exactly `"1"` is off); refusal is a *result*, not an exception, so the mission still learns why. RDAP is third-party data, so the returned contact is validated as strictly as a model-supplied one before becoming an envelope recipient. Missing contact degrades gracefully (§12/§13: "not published" is a finding, not a crash). Approval is never checked in-module — the harness gates it. **Qodo found 3 real bugs on first review, all mine, all fixed:** (1) a non-string RDAP `abuse_contact` (list/dict/int) raised `TypeError` out of `re.match`/`len`, breaking the "never raises" contract — confirmed by reproducing all three types; (2) a **DNS-rebinding window** — the guard resolved the host then handed the *hostname* to `smtplib`, which resolves it again, so a hostile resolver could swap in a routable address the guard never approved; now resolves once and connects to the validated loopback **literal**, and every `getaddrinfo` answer must be loopback; (3) the opt-in live test called **production `rdap.org`** and could never have shown a delivery anyway (a reserved `.example` domain publishes no abuse contact, so its own assertion was `sent: False`) — removed, with the root cause also fixed by moving the Range guard *before* the RDAP lookup so a refused send queries no third-party registry at all. **46 new tests** (30 unit + 15 contract + regressions); full `tools/` suite **198 passed / 3 skipped** from merged `main`, `harness` 18/18 unaffected — result: **merged into `main` as `71107ad33b22f793f022504dd0e91dab8fbb2696` (PR #40)** with Qodo clean of Bugs on the exact final HEAD `32c45c6` (0 Bugs / 0 Skill insights; the single remaining Rule violation is the deliberately accepted PR-size exception, documented in the PR on the PR #4/#19 precedent, because splitting would separate a mail-sending tool from the tests that are its only safety evidence). **Verification stated precisely: a REAL `smtplib` send over a REAL loopback TCP socket delivered successfully (evidence reference observed on the wire), which proves the SMTP path works unmocked — it does NOT prove a real abuse report was delivered to a registrar, and none was. No real registrar or abuse mailbox was contacted at any point.** The Mailpit live test could not be run: the Docker daemon was not running on this machine. Two of the four gated tools now exist; T-030/T-032 remain O2's and unstarted

2026-08-29 · T-040 (investigated, resolved without code) · [O1] · **Session persistence and resume are already provided by TrueForge — verified live, so there is nothing for us to build, and building it would have violated CLAUDE.md's own "don't rebuild what the harness already does" list (line 148 names *session persistence* and *reconnect survival* explicitly).** §5's T-055 row correctly flagged that nobody had ever actually checked TrueForge's live schema for a session-listing/history endpoint; this task checked. Against a real instance (WSL2), confirmed by response, not by reading docs: `GET /api/v1/sessions` returns **HTTP 200** with a real paginated list (`{"data":[],"pagination":{"limit":25}}`) and its summary in the live OpenAPI is literally "List sessions"; `GET /api/v1/sessions/{id}` and `GET /api/v1/sessions/{id}/events` are both real endpoints (proper `404 "Session not found"` for a bogus id, not a route miss); `GET /api/v1/sessions/{id}/turns`, `.../turns/{turn_id}`, `.../turns/{turn_id}/events` and `.../turns/{turn_id}/subscribe` all exist. **Persistence is on disk, not in memory:** the standalone instance keeps `~/.local/share/trueforge/db/db.sqlite`, and the file survived a full process restart with the server rebooting and serving `GET /sessions` again — so a campaign's sessions genuinely do survive a restart already. This **corrects an earlier O1 assessment** that called T-040 an invalid premise on the grounds that no such API had been confirmed: the API exists; what was wrong was the task's framing, not the goal. **The other half is still undefined and was deliberately not invented:** `dossier` and `campaign` appear **nowhere** in TrueForge's live spec (checked), §10's tool table has no dossier tool, no storage format or location is decided anywhere in this file, and §17's 3-minute demo script has no dossier beat at all. So "campaign dossier" remains an O1/O3 scope question (§8), not something to guess at — result: **no code written, none needed for the persistence half**; T-040's remaining scope is recorded in §8 and the task stays in §11 rather than being deleted or falsely marked complete. **Directly unblocks O3's T-055** (§5): its blocker's reason #2 — "no session-listing/history endpoint has ever been confirmed to exist on TrueForge's live schema; nothing in this repo has checked for one" — is now disproven with live evidence, and the Cockpit dossier view can bind to these endpoints. T-055's other stated reasons (no decided storage format, no §17 demo beat) are untouched and remain O3's call

# §5 BLOCKED

| ID | Owner | Blocked on | Since | Who can unblock |
|---|---|---|---|---|
| T-015 live-fire test | O1 | No model provider configured anywhere (§1 DO THIS FIRST still unchecked). Gate wiring itself is proven (§4); what's missing is watching a real turn actually emit `tool.approval_required` and resuming it | 2026-08-25 | Whoever configures a model provider key + spend cap in TrueForge's settings UI (`localhost:8790`) — enter it there, never paste it into chat |
| T-023/T-024 runtime verification | O1 | The `instructions` field is accepted-schema-safe (same as T-013/T-017), but that's not the open question — the open question is *behavior*: whether a real turn actually spawns three subagents matching these names/remits, whether an IDENTITY-labeled one stays tool-free in practice, and (T-024) whether reports actually come back as structured JSON instead of prose, given `dynamic_sub_agents` enforces none of it (§4, Qodo pass) | 2026-08-26 | Whoever next has TrueForge running locally + a model provider key: run a real turn against a suspicious-email fixture and observe the actual `create_sub_agent` calls/names/tool use/report format, not just POST `harness/agent.json` and check the status code |
| T-022 ↔ T-060 mailbox protocol | O2/O4 | **Corrected 2026-08-28** — the original version of this row was wrong in a way Qodo's PR #23 review caught: it offered "add an IMAP port to PR #22" as a fix option. That's not possible — Mailpit **does not implement an IMAP server at all**; this isn't a missing port, it's a capability the tool doesn't have (Mailpit's own maintainer states this directly, `github.com/axllent/mailpit/issues/72`, and its official feature list at `mailpit.axllent.org/docs/` names only SMTP, a web UI, and a REST API). T-060 is now merged (`f9c18f6`, 2026-08-28) — confirmed directly against `main`'s actual `range/docker-compose.yml`: Mailpit `v1.31.0`, ports `8025` (HTTP UI/API) and `1025` (SMTP) published, no IMAP port, no auth configured anywhere in the compose file or the vendored `range/mailpit-api.json` spec (no `securityDefinitions`). `T-022`'s own task name ("IMAP search for prior contact") is stale against what's actually merged. **Two real paths, neither chosen here:** (a) `correspondence_history` queries Mailpit's existing HTTP API (`range/mailpit-api.json` documents its `/api/v1/` search endpoints) — no new infrastructure, works with what T-060 already shipped; (b) introduce a separate IMAP-capable mailbox service alongside or instead of Mailpit for this one tool — real new infrastructure, crosses into O4's `range/` territory, needs a heads-up per CLAUDE.md's cross-folder rule. Rejected: "just add an IMAP port" — not a real option, see above | 2026-08-27 (opened) / corrected 2026-08-28 | O2 (owns T-022) and O4 (owns T-060/`range/`) need to explicitly pick (a) or (b) — record the decision and its reasoning in §6 once made; this row intentionally still isn't deciding it |
| T-055 dossier view | O3 | **Blocked — its core data dependency (persisted past missions, cross-mission campaign linkage) doesn't exist anywhere in this codebase, and building one ourselves would violate CLAUDE.md's own "don't rebuild what the harness already does" list, which names *session persistence* explicitly.** Verified before recording this: (1) **T-040** (O1, "Session persistence + resume — campaign dossier survives a restart") is still unpicked in §11 — no code, no design. (2) T-002's confirmed live-API spike (§4/§6) only established single-session/turn mechanics — create a session, stream a turn, resume an approval, reconnect via `GET /turns/{id}/subscribe?after_sequence_number=`. No session-*listing*/history endpoint has ever been confirmed to exist on TrueForge's live schema; nothing in this repo has checked for one. (3) §10's architecture diagram says the agent should "write campaign dossier," but no MCP tool for that appears anywhere in §10's own tool table, and no storage format or location is decided anywhere in PLAN.md. (4) §17's demo script has **no beat for a dossier view at all** — not present in the 3-minute script, so §11's own refill rule ("current slice first, then what unblocks other owners, then what the demo script needs") doesn't favor it either. **Explicitly not done as a workaround:** no Cockpit-side localStorage/database persistence was built to fake this, and no undocumented TrueForge API was guessed at — both would be exactly the kind of invented architecture CLAUDE.md warns against. T-055 stays a real future task, not disproven like T-025 — it becomes buildable once T-040 (or a confirmed TrueForge history API) exists | 2026-08-29 | O1 (owns T-040) needs to build/configure real session persistence, or whoever confirms TrueForge's live schema exposes a session-history endpoint T-055 could read from instead — until one of those two exists, this stays blocked |
| T-035 sandbox execution | O1 | **Blocked on a Daytona API key, and half the task's wording is unsupported — verified read-only against the live TrueForge OpenAPI schema and live endpoints, not inferred.** (1) Daytona *is* TrueForge's sandbox provider (`SandboxProviderManifest.type` is `enum ["daytona"]`; the catalog returns exactly one entry), and its `auth.api_key` is **required** — but `GET /api/v1/settings/sandbox-providers` returns `404 "No sandbox provider configured"`, and no key exists in the project (no `DAYTONA_*` reference tracked anywhere, `.env.example` holds only `URLHAUS_AUTH_KEY`). With no provider, `config.sandbox.enabled: true` yields no sandbox at all, so nothing can execute in one and nothing can be live-verified. Same shape as the T-015 model-provider blocker above. §8 already recorded this on 2026-08-25 ("Daytona credentials we don't have") — still true. (2) **No snapshot/warm-pool mechanism exists to configure**: `snapshot` appears twice in the whole spec and both are session/registry-agent-name binding, unrelated to sandboxes; `warm`, `pool`, `prewarm` are absent entirely. The only lifecycle levers are three tenant-level timers — `auto_stop_interval_in_minutes` (default 5), `auto_archive_interval_in_minutes` (60), `auto_delete_interval_in_minutes` (7200). Raising `auto_stop` is the one supported way to keep a sandbox warm between detonations (trap #4), but it is a `PUT /api/v1/settings/sandbox-providers` tenant setting with nowhere in the repo to commit it, and it still needs the api_key. (3) **"The Daytona sandbox job" has no definition**: the phrase appears only in O1's ownership lines (§10 repo layout, §10 owners table) and in T-026's description — no interface, no shape, no spec anywhere. TrueForge exposes no job-submission API; the sandbox is session-scoped and harness-owned (`SandboxCreatedEvent`: "sandbox is session-scoped"), which CLAUDE.md lists under "don't rebuild what the harness already does". (4) Consequently **T-026 ↔ T-035 ordering is unresolved** — T-026 is specified as "dispatching O1's sandbox job", i.e. it depends on a thing T-035 was supposed to produce but which nothing defines. Neither task was touched. **T-035 stays a real future task — blocked and needing scope clarification (§8), not disproven like T-025** | 2026-08-28 | Whoever obtains a Daytona API key (enter it via TrueForge's own settings, never paste it into chat or the repo) **plus** an O1/O2 decision on §8's scope questions — without both, T-035 cannot be implemented or verified |
| T-032 block-rule destination | O1/O2 | **Blocked on an undecided scope question, not on a missing key or a third-party outage: `create_block_rule` has no target system, and nothing would read the rules it creates.** Verified read-only against current `main` before recording: (1) the *contract side already exists* — `create_block_rule(pattern)` is in §10's tool table marked gated, `contracts/events.ts` already lists it in `ProposedActionName` (so `/contracts` would need no change and its 2-approval rule is not engaged), and it is already one of the four names in `harness/agent.json`'s `require_approval_for_tools` from T-034. (2) **No target system is specified anywhere.** Unlike its siblings — T-030 says "over IMAP", T-031 said "over SMTP", T-033 said "to the RDAP abuse contact" — T-032's §11 line is just `create_block_rule`, with no transport named; its only other mentions are §10's tool-table row and §10's diagram beat (`3. block rule → [Allow] [Deny]`), neither of which names a destination. (3) **Mailpit cannot be that destination:** the vendored `range/mailpit-api.json` has **zero** rule/filter/block endpoints (its surface is messages, tags, search, send, chaos, info). (4) **No blocklist or rule store exists in the repository** — every grep hit for the name is the *gate configuration* (`agent.json`, `agent-config.test.js`, `server.py`'s registration, `contracts`, `missionSource.ts`, `harness/README.md`), not a store; and **no `tools/imports_mcp` module writes to disk at all**, so there is no persistence precedent to follow. (5) **Nothing consumes or enforces a block rule** — `contracts/events.ts` references it only as an action *name*; no component reads rules. Implementing it today would mean inventing both a storage format and a store that nothing ever reads. **Note what this blocker is NOT:** it is *not* the undecided Mailpit/IMAP mailbox protocol that blocks T-022 and T-030 — a pattern-based rule needs no mailbox, which is why T-032 was never listed alongside them. This is an independent, and smaller, question | 2026-08-29 | An explicit O1/O2 project decision on where a block rule lives and what (if anything) enforces it. A self-contained local rule store owned by `imports-mcp` is one plausible answer — the §17 demo beat is arguably the *gate*, not the enforcement — but that is a scope decision requiring explicit approval, **deliberately not taken here**: no storage format, API, enforcement behaviour or demo semantics has been invented (§8) |

> **Resolved, removed from the table above (2026-08-27):** `DomainIntel`/`UrlReputation` contract fix — blocked 2026-08-27, unblocked the same day by PR #19 commit `3f169b3`. The original blocking evidence (the exact mismatch, both direct-inspection and Qodo's independent confirmation) and the resolution are both recapped in §4 (2026-08-27, "T-020 (`DomainIntel` contract resolved..." entry) — the byte-exact original row text is preserved in git history (this commit's diff, and PLAN.md as it stood immediately before it) rather than restated verbatim here. `UrlReputation` was never actually blocking (narrower than reality, not breaking) and stays that way — only the `DomainIntel` half needed a fix.

> Note (2026-08-29, O1 — partial correction to **T-055's** row above, not a reassignment of it): that row's reason #2 said "no session-*listing*/history endpoint has ever been confirmed to exist on TrueForge's live schema; nothing in this repo has checked for one." It has now been checked (T-040, §4) and **the endpoints do exist**, confirmed against a live instance by response rather than by docs: `GET /api/v1/sessions` → HTTP 200, paginated, summary "List sessions"; `GET /api/v1/sessions/{id}` and `/sessions/{id}/events` real (proper 404 for a bogus id); `/sessions/{id}/turns`, `.../turns/{turn_id}/events`, `.../turns/{turn_id}/subscribe` all present; and the standalone instance's `db.sqlite` survives a full restart, so past sessions genuinely persist. **T-055's other stated reasons are untouched and still stand** — no decided storage format for a *campaign dossier*, no dossier tool in §10's table, and no dossier beat in §17's script — and whether that leaves T-055 unblocked is O3's call, not this note's. Flagged here because the row rested partly on a fact that is no longer true.

---

# §6 DECISIONS

> One line each, dated, **with the reason**. Logged the moment a decision is made, never batched.
> Any argument running past 10 minutes goes to Owner 4, gets one line here, everyone moves on.

```
2026-08-24 · [all] · Project is UNIVERSAL IMPORTS — email counter-intelligence — the sandbox is genuinely load-bearing, there are four natural approval gates, and no other team wires both sponsors together
2026-08-24 · [all] · Fake portal runs INSIDE the Daytona sandbox — Daytona is remote and cannot reach localhost on our laptops
2026-08-24 · [all] · IMAP + app password, never the Gmail API — gmail.modify is a restricted scope costing a day in consent screens and review
2026-08-24 · [all] · Read the Authentication-Results header, never verify DKIM ourselves — DNS + canonicalisation is a two-day rabbit hole
2026-08-24 · [all] · PhishTank excluded — registration closed since 2020, still closed in 2026
2026-08-24 · [all] · URLhaus is one weak signal only — it is malware-focused, not phishing-focused, so "not listed" does not mean safe
2026-08-24 · [all] · Demo script (§17) written before the code — any feature that can't name its second in the video doesn't get built
2026-08-24 · [all] · Approval gates are FOUR SEQUENTIAL per-tool-call gates, not one modal with four checkboxes — TrueForge's native approval is per tool call, boolean, and shows the JSON request. We configure it in agent.json, we do not build it. Four stopping moments is also more cinematic than one modal
2026-08-24 · [all] · Sandbox lifecycle is TrueForge's job, not ours — we only write what runs inside it. Spike 1 must measure SECOND-RUN time, not just whether chromium installs: a 4-min install that works in testing dies on camera
2026-08-24 · [all] · Qodo installed and left alone — it is half of judging criterion 04 which is scored on EVERY submission regardless of track. 10 min setup, zero ongoing. We are NOT chasing the Best Code Quality track itself; four iPads on Best UI beats one Mac Mini split four ways
2026-08-25 · [O1] · Detonation defaults to TEXT-MODE FALLBACK (HTTP redirect chain, HTML parse, form-target extraction) — Spike 1 (chromium in Daytona) never produced committed work inside its 3h box, box expired ~9h unattended. Screenshot detonation is now a stretch goal only, revisited if Slice 1–3 land early
2026-08-25 · [O1] · Cockpit (O3) builds against the HTTP/SSE API directly, never the chat UI — POST /sessions, POST /sessions/{id}/turns (stream:true → SSE), watch for `tool.approval_required` events, resume with a turn whose input is `{type:"user.tool_approval", thread_id, tool_call_id, approval:{status:"allow"|"deny"}}`. Reconnects use GET /turns/{id}/subscribe?after_sequence_number=. Confirmed against the live server's own OpenAPI schema, not just docs (T-002)
2026-08-25 · [O2] · RDAP lookups go through `rdap.org/domain/{domain}` with redirects followed, not hardcoded per-registry servers — one entry point, confirmed fast (~0.5s) and correct
2026-08-25 · [O2] · URLhaus now requires the `Auth-Key` header on every endpoint, including read-only host/url lookups, not just submissions — code the intel client assuming auth is mandatory everywhere
2026-08-25 · [O2] · `imports-mcp` is Streamable HTTP via the official **Python** MCP SDK's `MCPServer` — **not** Express/JS. The `bring-your-own-mcp` cookbook (which is Node/Express) only settled the transport *shape* (Streamable HTTP, not stdio); the language/SDK is Python per the decision directly below, since `/tools` is Python. Runs `stateless_http=True` together with `json_response=True` — the default SSE response mode was later found (T-012) to hang forever on a tool error under statelessness, logged in §7 once that lands — registered in TrueForge via Settings → Connectors then referenced by name in `agent.json`
2026-08-25 · [O2] · `/tools` is Python — stdlib `email` for RFC822 parsing, `lxml` for HTML (per the trap in §12), MCP server on the official Python MCP SDK's streamable-HTTP transport to match T-004's findings
2026-08-26 · [O3] · Contract types must be modeled on the real producer's actual output, not the tidiest shape that fits the architecture diagram — Qodo's PR #12 review caught `DetonationResult` inventing field names (`asks_for_password`/`posts_cross_domain`) that don't exist in `harness/detonate.js`. Going forward, any `contracts/events.ts` type wrapping an existing producer reads that producer's code first, PLAN.md §10's table second
2026-08-26 · [O3] · `contracts/fixtures/mission-happy-path.json` represents all four licence gates as **granted** — "happy path" read literally as the full mission completing with nothing blocked. §17's specific "allow, allow, deny, allow" sequence is a deliberate dramatic beat for the demo video, not this base fixture. A separate fixture (e.g. `mission-with-denial.json`) can be added later if Cockpit needs to test the deny-rendering path before the real gate demo is rehearsed
2026-08-26 · [O1] · Qodo's PR #4 (T-015) review re-checked against the correct base: `.gitignore` and all of `detonate.js`/`detonate.test.js` landed in PR #3 (main), so findings #1, #4, #5, #6, #7 from the review attached to stale commit 5a31de1 do not apply to PR #4's actual diff at all — nothing to fix there, no rebase needed (PR #4 already sits on current main). Fixed the three findings that are real for this PR's diff: `quarantine_stub`'s response now caps `message_id` to stay under the ~2KB MCP limit (Rule 2880706) with a `truncated` flag and a regression test; added `harness/test/approval-gate-verification/` (throwaway test-agent payload + script) so the T-015 gate-wiring proof is reproducible from a clean checkout, `harness/agent.json` still untouched. Left the 400-line PR-size finding (Rule 2880655) as a documented position, not a code change — see next line | Findings #1/#4/#5/#6/#7 N/A to PR #4; #3 and #8 fixed with tests; #2 documented |
2026-08-26 · [O1] · **PR #4 size (Rule 2880655) — documented, not resolved by deleting anything.** `harness/package-lock.json` is machine-generated by `npm install` from `package.json`'s 3 declared dependencies (MCP SDK, zod, and their transitive tree); it is not hand-authored and isn't meaningfully reviewed line-by-line. The actual hand-authored diff in this PR (`package.json`, `stub-mcp-server.js` + its new test, the approval-gate-verification throwaway files, README/PLAN updates) is well under 400 lines; the lockfile alone accounts for the overage. Deleting the lockfile to shrink the diff was rejected — it would break reproducible installs (`npm ci`), which CLAUDE.md requires we not do for a "required dependency file." Documented on the PR description for Qodo/reviewers instead, same posture as the Rule 2880752 precedent above | Documented in PR #4 description; lockfile kept |
2026-08-26 · [O1] · **Rule 2880752 (localhost/SSRF) resolved differently on PR #4 than on PR #3 — a real fix, not another decline.** PR #3's decline (row above, 2026-08-25) still stands as the correct call for *that* PR: `detonate.test.js`'s fixture is a same-process unit-test double with no Daytona integration to violate. On re-evaluation for PR #4, though, Qodo's evidence pointed at a genuine gap independent of Daytona: `detonate()` had no check that the initial URL or a redirect resolves to a private/internal address, which is a real SSRF risk in production regardless of sandboxing. Fixed with a resolved-IP guard (loopback/RFC1918/link-local/cloud-metadata refused by default) plus an explicit `allowPrivateNetworkTargets` opt-in for the test fixture only — no Daytona credentials needed, no fabricated dependency | Implemented in `detonate.js`; PR #3's original decline entry left unmodified as the historical record for that PR |
2026-08-26 · [O1] · Findings #4 (.gitignore) and #7 (approval-gate reproducibility) dismissed on PR #4 via `@qodo dismiss` rather than further code changes — both were already substantively fixed (PR description, `harness/test/approval-gate-verification/`) and Qodo's incremental re-review doesn't refresh evidence for findings whose originally-cited files weren't the ones edited (see §7). Qodo's own chat reply independently confirmed #7's evidence was stale before the dismissal | Dismissed with reasoning on each finding's thread, not code-changed |
2026-08-26 · [O1] · T-020's PR #9 was split into two PRs (#9 + #14) purely to satisfy the 400-line rule (§15), not because `test_domain_intel.py`'s exhaustive unit-test suite was optional — confirmed directly from the split commit's own message: "...so it's still reviewed as part of finishing T-020's Qodo pass — just as its own PR... The file isn't deleted, only untracked here; it reappears whole in the next branch." Both halves were required for T-020 to be complete: PLAN.md's own §4 record already claimed test coverage (the JSON-decode-failure regression tests, the corrected malformed-events test) that only physically existed in PR #14 before it merged. Both PRs merged — see §4 | Both required, both merged; treat a "PR split for line-count" as splitting one task's review, not splitting what's actually required to ship it |
2026-08-26 · [O2] · PR #10 (T-021) merged under an explicit, one-time authorized exception to the "never merge before Qodo has reviewed" rule — Qodo never produced a review or check-run for the exact final HEAD (`30b8bd7`) after a rebase, force-push, and base retarget, confirmed repeatedly with no change. All other merge preconditions were independently re-verified immediately beforehand (mergeable/clean, correct base/head, 15/15 T-021 tests, 24/24 T-020 regression tests, 80/80 full suite, diff scoped to T-021's own files). This is a one-off for PR #10 only — it does not authorize skipping Qodo review on any other PR |
2026-08-27 · [O3] · Cockpit stack: Vite + React + TypeScript, no state-management/UI-kit library. Nothing in PLAN.md/CLAUDE.md picked a frontend stack before now, so this was T-050's own small spike, not a preference — picked because it's the simplest option that (a) imports `contracts/events.ts` directly, satisfying that file's own "Cockpit code imports types from here, never redeclares them" rule (TS + a bundler that resolves a cross-folder `../../contracts` path — plain JS or a non-TS framework couldn't do this cleanly), (b) needs zero extra deps to prove the T-050 scope (fixture in, events out, rendered), and (c) doesn't foreclose T-051-056's componentized, multi-panel, streaming-update needs the way reaching straight for vanilla DOM manipulation would. State management/routing/UI-kit deliberately left out — not needed yet, add only when a real task needs one
2026-08-27 · [O2] · `parse_message`'s `from`/`reply_to`/`return_path` are plain address strings (not "Display Name <addr>", not a nested object) and `display_name` is its own top-level field taken from the From header only — the shared `ParsedMessage` contract expects a name for the sender alone, and every other address field is `string | null`. A message legally carrying more than one `Authentication-Results` header (one per hop that added a check) has them joined with `\n` into a single string rather than kept as a list — CLAUDE.md trap #2 only requires reading the raw text, never verifying it, and a joined string still preserves every hop's text losslessly
2026-08-27 · [O2] · `domain_intel`'s server-side tool wrapper rejects any domain that isn't RFC-1035 hostname syntax (labels of 1-63 letters/digits/hyphens, dot-joined, ≤253 chars total) before it ever reaches `_domain_intel` — a caller-supplied `/`, `?`, or `#` would otherwise change which RDAP path/query actually gets requested. Validation lives in `server.py`, not `domain_intel.py`, so the underlying module stays testable with any string a caller passes it directly (as the existing unit tests already do)
2026-08-27 · [O2] · Every `imports-mcp` tool's response-size cap must strictly shrink every upstream-controlled scalar string toward `""` (never plateau at a fixed floor like `max(1, len(value) - 200)`) and must include every field the JSON actually needs to serialize (e.g. the `truncated` flag itself) in the size check *before* deciding the payload already fits — both were real bugs Qodo caught on PR #19 (a hang risk and a silent over-budget response respectively), and both are now the required shape for any future tool's own cap function, not just url_reputation's/domain_intel's
2026-08-25 · [O2] · Any Python code in `/tools` making HTTPS calls must `import truststore; truststore.inject_into_ssl()` before the first request — see §7, this isn't optional on every machine
2026-08-25 · [O2] · crt.sh results are cached in-memory only for now (module-level dict, cleared on restart) — good enough for a single mission run; T-045 upgrades this to SQLite, don't build that early
2026-08-25 · [O2] · Raise `mcp.server.mcpserver.exceptions.ToolError` for anticipated tool failures, not a bare exception — a bare exception gets wrapped into a generic "Error executing tool X" and the real reason never reaches the model; `ToolError` preserves the message and skips the noisy traceback log
2026-08-26 · [O2] · `domain_intel`'s ~2KB response cap shortens free-text fields (registrar, notes, etc.) rather than dropping structured ones (`available`, `age_days`, dates) when the serialized payload is over budget, and always sets an explicit `truncated: true|false` — every future `imports-mcp` tool with unbounded upstream/caller-controlled text should follow the same shape, not invent a new one
2026-08-26 · [O2] · crt.sh cache entries are stored as `(cached_at, result)` and checked against `CRTSH_CACHE_TTL_SECONDS` on read, not just written once and trusted forever — a cache with a declared TTL that's never actually checked is worse than no cache, since it looks correct in a quick read of the code
2026-08-26 · [O2] · Normaliser's URL contract is now: every link from every multipart alternative (not just HTML), deduplicated by href, trailing sentence punctuation stripped, HTML hrefs restricted to absolute `http(s)` only — this is the shape `url_reputation` and any future consumer should expect, settled fixing Qodo's PR #6 findings
2026-08-28 · [O3] · T-051's Mission View is now a plan tree (§10's 6 stages: message → evidence [3 fixed lanes] → detonation → verdict → 4 licence gates → complete), not T-050's flat event log — every stage/lane/gate slot renders from event 0 (pending until its own events land), not just what's arrived so far. Tree shape comes only from §10's architecture and the contract's own `gate_index: 1|2|3|4`, never invented per-mission structure; a live TrueForge stream is read the same way the fixture is. Added a 4th node status, `denied`, distinct from `done`, for a gate a human rejects — §17 names the denial as the shot that proves the gate is real, so it can't render identically to a normal execution
```

---

# §7 ERRORS / SURPRISES

> Anything that cost someone time, so nobody pays it twice.

| Date | Owner | What bit us | What to do instead |
|---|---|---|---|
| 2026-08-25 | O1 | T-001 picked, timeboxed, session ended with no commit — nothing landed in git, no files, no findings. ~9h lost to Day 0 gate before anyone noticed the box had expired | Commit as you go, not just at task end. If a session might end mid-task, commit a WIP note to PLAN.md §3 at minimum so the next session can see real elapsed state, not just a stale timestamp |
| 2026-08-25 | O1 | `npx @truefoundry/trueforge` **segfaults on native Windows**, reproduced twice, right after it logs `Local sandbox fallback is unavailable (win32 not supported)`. Running it from WSL2 without Node installed *inside* the Linux distro just falls through to the Windows node.exe via interop — same crash | Whoever is on Windows: install Node inside WSL2 Ubuntu itself (`curl -fsSL https://deb.nodesource.com/setup_22.x \| sudo -E bash - && sudo apt-get install -y nodejs`, or nvm) and run TrueForge from there, not from Windows or from WSL-with-Windows-node. **Fixed — T-017 done, confirmed working** |
| 2026-08-25 | O1 | Driving `wsl.exe` from this Bash tool (which is Git Bash/MSYS) silently mangled `/mnt/c/...` paths — MSYS rewrites leading `/` args before handing them to native exes, turning `/mnt/c/...` into `C:/Program Files/Git/mnt/c/...`. Also: separate `wsl -d Ubuntu -- bash -lc "..."` calls don't share state — WSL2 tears the instance down ~8s after the last process exits, killing anything backgrounded with plain `&`/`nohup` between calls | Prefix the command with `MSYS_NO_PATHCONV=1` to stop the path rewrite. Do the whole start-and-poll-and-verify sequence in **one** `wsl` invocation (one script), not several — that's also what actually needs `setsid ... </dev/null` to background cleanly within that one call |
| 2026-08-25 | O1 | `harness/README.md` fell out of date **within the same PR** — T-017 fixed the Windows segfault and runtime-verified `agent.json`, but the README's "known issue" section still read as unresolved. Qodo's first-pass review caught it (2 Medium findings) rather than us | When a task's result changes something already documented elsewhere in the same branch, update that doc in the *same commit* as the fix, don't leave stale wording for review to catch |
| 2026-08-25 | O1 | Running `node`/`npm` from WSL2 against this repo (`/mnt/c/...`, which is also OneDrive-synced) is **pathologically slow for anything touching `node_modules`** — a plain dynamic `import()` of an installed package hung 8+ seconds with zero output, no error. Copying the same `node_modules` to WSL's native filesystem (`/tmp/...`) and running from there: instant, worked first try | For any Node work done from WSL against this repo: either `npm install` and run from a native-WSL path (`/tmp/...` or `~/...`), copying source in, not `/mnt/c` directly — or accept real slowness. TrueForge itself is unaffected (its own `npx` cache lives in the WSL user profile, not `/mnt/c`) |
| 2026-08-25 | O2 | crt.sh returned 502 on 7/7 live attempts against two different domains, in-line with the known trap | Ship the 5s timeout + SQLite cache from day one (T-045) — don't wait for it to fail in front of a judge |
| 2026-08-25 | O2 | No `.env.example` existed even though a real `.env` with `URLHAUS_AUTH_KEY` was already in the repo (gitignored, untracked — never committed) | Added `.env.example` with the key name and no value; do this for every new secret going forward |
| 2026-08-26 | O1 | Assumed Qodo's incremental PR review re-diffs against current `main` on every push, same as GitHub's own Files-changed tab. It doesn't: after pushing a commit that only touched 5 files (confirmed against GitHub's own `files` API), Qodo's updated review still cited `detonate.js`/`detonate.test.js`/`.gitignore` line ranges as PR evidence — files untouched since PR #3 merged and absent from GitHub's actual diff. It re-scans the full file tree at HEAD against its standing rule set, not a git diff, so account-level rules (2880752, 2880666) re-fire on any file in the tree regardless of whether this PR's own commits touched it. Worse: even after the underlying fix (PR description, new files), the displayed finding stayed unchanged/stale across two more HEAD updates — only a `@qodo dismiss` reply on the finding's thread flips its displayed status; there's no "refresh evidence" trigger | Before treating a Qodo finding as "not this PR's problem," check GitHub's actual PR `files` list (`gh pr view --json files`), not just local `git diff main...branch`. If a finding is genuinely fixed but Qodo still shows it open with unchanged evidence, don't chase it with more code changes — dismiss it on its own thread with the current evidence cited |
| 2026-08-26 | O1 | T-020's §4 DONE entry ("Qodo pass 2," written before the PR split) claimed specific tests existed and passed ("1 existing test corrected... plus 2 new tests... 37/37 total pass"), but the commit that entry describes (`8a3d4e0`, PR #9's own final commit) touches only `PLAN.md` and `domain_intel.py` — zero test files. The tests the entry claims only existed in the later PR #14 split. Anyone reading PR #9 alone, without also checking PLAN.md's git-blame or the sibling PR, would have no way to tell the claimed coverage wasn't actually in that PR | When a task's Qodo-fix pass gets split across two PRs mid-stream, write (or amend) the §4 entry to say explicitly which PR each part landed in — don't let one entry's claimed test count silently span code that ends up in a different PR than the one being reviewed |
| 2026-08-26 | O2 | Distinct from the "stale evidence" issue above (§7, 2026-08-26/O1): after force-pushing a rebased branch (`--force-with-lease`) and then retargeting the PR's base branch via the API, Qodo produced **no** review or check-run at all for the new HEAD — not stale evidence, no evidence, confirmed by 0 `check-runs`, commit status `pending`, and no new PR comment, checked repeatedly over time. A normal push seems to trigger Qodo fine; a force-push combined with a base retarget apparently did not | If a PR's history is rewritten (rebase/force-push) and/or its base is changed, don't assume Qodo will automatically re-review the new HEAD the way it does for an ordinary push — check for an actual new review/check-run before relying on one, and budget time to trigger it manually or escalate if it never appears |
| 2026-08-26 | O1 | PR #12 (T-016) merged into `main` at `cbe5d52` (2026-08-26T10:03:59Z), but §1/§4/§5 kept saying "still needs a 2nd human approval, not merged" through two separate later conflict-resolution merges on two different branches (PR #13 twice, plus main's own PR #15 sync) — nobody re-checked `gh pr view` against the actual claim, they just carried the existing text forward while resolving unrelated conflicts | When resolving a PLAN.md conflict that touches a PR's merge status, verify that status against `gh pr view <n> --json state,mergedAt,mergeCommit` before carrying either side's wording forward — a conflict-resolution merge is exactly the moment stale status silently propagates, since neither side's author knew the other had gone stale |
| 2026-08-27 | O1 | T-024's first draft designed IDENTITY's structured-evidence field list straight from T-023's old *analysis* prose ("compare display name against Reply-To and Return-Path") without re-checking it against `contracts/events.ts`'s already-existing `IdentityEvidence` type — result: missing the contract's required `from_address`, plus an extra `return_path` field the contract never had. Unlike `DomainIntel`/`UrlReputation` (§8), `IdentityEvidence` has no separate real producer to diverge from — it's derived from parsed-message fields same as this prompt is, so the contract *was* the authoritative shape here and got overlooked anyway | When a task's output has an already-defined contract type (check `contracts/events.ts` and its consumer's runtime validator, e.g. `cockpit/src/missionSource.ts`), design directly from that type's field list, not from older prose describing the *analysis*, not the *output shape* — the two can drift even when both were written by the same task |
| 2026-08-26 | O2 | Fixing Qodo's URL-punctuation finding on the normaliser, the first version of the trailing-punctuation stripper ran as two separate passes (strip excess closing parens, then strip trailing punctuation) and missed punctuation *after* an excess paren — `"(see http://x.com)."` came out as `"...x.com)"` instead of `"...x.com"`. A regression test caught it immediately, before the fix was pushed | When trailing characters can be stripped for more than one reason (punctuation vs. an unbalanced paren here), use one loop that re-checks the string after every removal, not sequential fixed passes — order-dependent stripping bugs hide at the boundary between the two rules |
| 2026-08-25 | O2 | `requests` failed SSL verification on every HTTPS call (`CERTIFICATE_VERIFY_FAILED: unable to get local issuer certificate`) even though `curl` on the same machine worked fine. Root cause: Norton Antivirus does local TLS inspection and generates its own root CA (`Norton Web/Mail Shield Root`), which Windows trusts (curl uses the OS store via schannel) but `certifi`'s fixed CA bundle does not | Add `truststore` and call `truststore.inject_into_ssl()` before any HTTPS request — it points the stdlib `ssl` module (and therefore `requests`/`httpx`) at the OS trust store instead of `certifi`. **Not a one-off**: any teammate running AV with TLS inspection will hit the exact same wall on any Python HTTP call, not just this one |
| 2026-08-25 | O2 | `imports-mcp` server hung forever (never returned, client never timed out) on the very first error-path tool call — `stateless_http=True` combined with the default SSE response mode (`json_response=False`) apparently never closes the event stream when a tool raises. Cost ~15 min to bisect with a background process + hard-timeout client before finding it | Pass `json_response=True` alongside `stateless_http=True` for any single-purpose stateless MCP tool server — plain JSON responses, no SSE needed for a request that's just "call one tool, get one answer" |
| 2026-08-25 | O2 | `python` on PATH in Git Bash resolves to an MSYS2 build (`C:\msys64\ucrt64\bin\python.exe`) with no PyPI wheels for compiled packages — `pip install lxml` tried to compile from source and failed on a missing `libxml/xpath.h` | Use the python.org install instead (`py -3` or the full path under `AppData\Local\Programs\Python\`) when creating `tools/.venv` — every O2 teammate on Windows needs to know this |
| 2026-08-26 | O2 | RDAP/crt.sh parsing only caught JSON *decode* failures — a technically-valid response with the wrong shape (e.g. `"events"` as an int instead of a list) hit `.get()`/iteration on the wrong type and raised, and because `domain_intel()` called RDAP and crt.sh with no exception boundary between them, an RDAP crash would have taken crt.sh down with it too, breaking the "independent degradation" promise for both sources at once | Never trust a public API's response shape past "valid JSON" — add `isinstance` checks at every level you're about to call a type-specific method on, and wrap the parsing (not just the request+decode) in a try/except that degrades to `available: false` |
| 2026-08-26 | O2 | PR #9 grew to 692 changed lines by the time Qodo's 5 findings were fixed (started at 451, already over) — nobody had been tracking the running total against §15's 400-line rule while the fixes accumulated | Check the diff size *before* pushing a review-fix commit, not after Qodo flags it a second time; split immediately once a task's real scope (implementation + the tests it actually needs) is clear, don't wait for the rule violation to force it |
| 2026-08-28 | O3 | T-051's PR #32 was split (into #34 + #32) specifically to fix a genuine 400-line-rule violation, then #32 was rebased onto the new `main` after #34 merged. Qodo's *next* review of #32 still cited the pre-rebase diff size (464 lines) for the size finding specifically, even though it correctly picked up the rebased HEAD for every other finding (3 Bugs, all re-scanned and marked Resolved against real code) | A PR-size finding's own cited numbers can go stale after a rebase even when the rest of the same review pass is current — verify against `gh pr view <n> --json additions,deletions` (GitHub's own count, not Qodo's quoted text) before trusting a still-open size finding post-rebase; don't assume every finding in one review pass shares the same freshness |
| 2026-08-26 | O2 | `tools/tests/test_server_integration.py`'s full suite (all 6 tests, one shared module-scoped server subprocess) is flaky: running it end to end reproducibly hits `httpx.ReadTimeout` on a later test — the exact test that trips varies (domain_intel's live RDAP/crt.sh call, url_reputation's live URLhaus call, or even the local-only empty-url validation test after either) but it's always *after* a real network call has gone through that same subprocess. **Confirmed pre-existing** — reproduces on the unmodified pre-T-021-Qodo-pass code too, not caused by this PR's fixes. Not fixed here — out of scope for T-021's 4 assigned findings, and it's `test_domain_intel`/T-020 territory as much as T-021's. Every individual test passes cleanly in isolation | Whoever picks this up: suspect the server subprocess's event loop stalls (or a connection-pool/keep-alive issue) after a slow real-network call, not the client — investigate with the server's own stdout (already captured in the fixture) rather than re-guessing at the client side |
| 2026-08-27 | O1 | T-025 ("cheap model for subagents, strong model for the lead") was written into §11 at the project's initial commit, before any schema was checked against a live server or live docs, and sat unquestioned through two backlog refills into §2 as "a config change... not new code." Checked before implementing: `harness/agent.json`'s `dynamic_sub_agents` is `{enabled: bool}` only, `model` exists once at the manifest top level (root agent only); confirmed against TrueForge's own current docs (`trueforge.dev/create-agent/overview`'s full `manifest.config` field table) that no per-subagent model field exists anywhere in the schema. The task wasn't blocked or hard — it described a config knob that has never existed | Before pulling a backlog item that names a specific platform config field, check that field actually exists in the current schema/docs — don't trust a task description's phrasing ("config change, not new code") as proof the config exists, especially for items written at project inception before anything was schema-verified |
| 2026-08-28 | O3 | T-051's `missionPlan.ts` shipped with a lane-status bug (empty-lane-after-completion stuck `pending` forever) that Qodo's PR #32/#34 reviews never caught, because the fixture always populates all three evidence lanes — the buggy branch was simply never exercised by anything that got reviewed. T-052 refactored the same logic into a second, independently-reviewed export (`buildEvidenceLanes`) and Qodo caught it immediately on that fresh pass | A Qodo-clean review is clean against the code paths the diff/fixture actually exercises, not proof the logic is correct for every input the type signature allows — an edge case a fixture never hits can sit merged and unreviewed-in-practice until something else forces that code path to be looked at again. Don't read "Qodo found nothing" as "there's nothing to find" for branches the test data never reaches |
| 2026-08-28 | O3 | T-053's Qodo review found `missionPlan.ts`'s `detonationEvent`/`DetonationPanel.tsx` used `events.find()` (first match) instead of `events.findLast()` (current/latest) — fixed in both. While fixing it, noticed `missionPlan.ts`'s `messageEvent`/`verdictEvent` lookups use the identical `events.find()` pattern, the same latent bug class, on different event types — **not fixed here**, deliberately out of T-053's scope (detonation only) | Whoever next touches `missionPlan.ts`: `messageEvent`/`verdictEvent` should also be `findLast()`, not `find()`, for the same reason — a re-emitted message or verdict event should be treated as the current one, not ignored in favour of whichever arrived first. Small, mechanical, not urgent (no known trigger for either event repeating today), but real |

---

# §8 SUGGESTED CHANGES TO PLAN

> The plan is wrong sometimes. Write it here, raise it at standup, then edit the sections below.

| Date | Owner | Suggestion | Status |
|---|---|---|---|
| 2026-08-25 | O1 | Plan assumed `harness/agent.json` is a file TrueForge itself reads. It isn't — agents are created via `POST /api/v1/agents` with a `manifest` body (model, instructions, mcp_servers incl. `require_approval_for_tools`, config). We're keeping `agent.json` as our repo-committed source of truth for that manifest and seeding it with a `curl` POST — matches the spirit of "configuration, not code" (§10) just via API instead of a config file TrueForge auto-loads | Adopted, see `harness/README.md` |
| 2026-08-25 | O1 | Qodo's PR #3 review flagged `detonate.test.js`'s fixture server for using `127.0.0.1` as a rule violation, citing the "Daytona can't reach localhost" trap. **Declined, on reconsideration with fuller evidence.** The only documented rule (CLAUDE.md #9, PLAN.md §12, both verbatim-checked) says the *production fake portal* must live inside Daytona because a remote sandbox can't dial back to a laptop — a network-reachability fact about one specific target, not a blanket ban on localhost. Neither file mentions test fixtures. Implementing Qodo's literal remediation (host the test fixture inside a live Daytona sandbox) would need Daytona credentials we don't have, spin a sandbox per test run (directly violating CLAUDE.md trap #4: "never spin a fresh sandbox per detonation"), and break a judge's clean-clone test run (rule 5, T-065) — trading one claimed violation for three real ones. **What Qodo did get right, and what we missed the first pass:** §10's architecture diagram draws detonation (redirect chain, form targets — exactly what `detonate.js` does) as running *inside* Daytona in the intended production pipeline, and T-014's own backlog line calls the text-mode fallback "still genuine sandboxed work." So production `detonate.js` genuinely is meant to eventually execute inside a Daytona sandbox — that wiring doesn't exist yet (T-035, scope clarified below). That's a real, separate gap from this test-fixture question, out of T-014's scope/timebox as built. Replied on the Qodo thread with this full reasoning | Declined, reasoning posted on PR #3's review thread; T-035 scope clarified |
| 2026-08-25 | O1 | **The exact conflict on finding #3 (Qodo Rule 2880752), stated precisely.** Rule 2880752 ("Disallow running the detonation fake portal on localhost," `app.qodo.ai/rules/2880752`) is a *standing, account-level Qodo Compliance Rule* — not a per-review inference. It reads: "detonation-related fixtures are required to run at a Daytona sandbox endpoint," with no carve-out for test-only fixtures. This is broader than its source text (CLAUDE.md #9 / PLAN.md §12), which names one specific case: the *production* fake portal must be reachable *by a real, remote Daytona sandbox*, because Daytona can't dial back to a laptop. `detonate.test.js`'s fixture triggers neither the wording's condition nor its reason — the code under test (`detonate.js`) has no Daytona integration (§11 T-035, unbuilt) and isn't invoked from inside any sandbox in this test; both the fetcher and the fixture run in the same local Node process, so there is no remote-sandbox-to-laptop gap to cross. The conflict is therefore rule-scope vs. architecture, not evidence vs. architecture: satisfying the rule as written would mean building Daytona orchestration as a prerequisite for a task explicitly scoped as the fallback *because* that spike didn't land, and would itself break CLAUDE.md trap #4 and rule 5/T-065 (§8, row above). Rule stays as configured — no repo-level Qodo config exists to change (checked: no `.qodo*`/`pr_agent*` file in the repo; the rule lives entirely on the Qodo dashboard) — and no change was made to it. Declined via a `@qodo dismiss` reply on the finding's thread instead, since that records the decision on the PR without touching rule configuration | Declined; dismissal requested on PR #3, rule config untouched (needs explicit approval to change) |
| 2026-08-25 | O1 | Whoever's on Windows for real dev work should expect to run TrueForge from WSL2 (with Node installed inside the distro), not native Windows — it segfaults there. Worth a line in the top-level README's setup steps once written (T-065 checks this on clean clone) | Open |
| 2026-08-26 | O3 | §10's MCP tool table has no dedicated tool/shape for the IDENTITY lane ("display-name vs reply-to, lookalike domain" per the architecture diagram) — INFRASTRUCTURE maps to `domain_intel`/`url_reputation`/`detonate`, HISTORY maps to `correspondence_history`, but IDENTITY analyzes fields already present on the parsed message rather than calling a new tool. Added a minimal `IdentityEvidence` type to `contracts/events.ts`, worded directly from §10's own diagram text rather than invented. Update (2026-08-26, Qodo remediation): `EvidenceEvent` now pairs each lane to its valid evidence shape(s) as a discriminated union (Qodo PR #12 finding #4) — `identity` can only carry `IdentityEvidence`, enforced by the compiler, not just convention | Open, still needs O1/O2 confirmation the identity subagent needs no MCP tool of its own |
| 2026-08-26 | O3 | PR #12's line count (finding #5): round 1 only got it from 495 to 464, still over ~400. Round 2 found genuine remaining bulk in the fixture — mechanical/repetitive flat objects (gate approvals, evidence) formatted one-field-per-line for no real readability gain — and collapsed them to single-line objects, still valid indented JSON, no event/field dropped. `/contracts` diff: 464 → 365; total PR diff now under 400. Lesson: check for real formatting bulk before concluding a size limit can't be met | Resolved |
| 2026-08-26 | O3 | PR #4 (`harness/approval-gate-stub`) and the PR #5 tools stack (`tools/spike-intel-apis` onward) have each independently edited §2 NEXT UP / §11 backlog differently while both unmerged — PR #4 added T-010 to NEXT UP while the tools stack was already doing T-010 for real, and the two disagree on T-016's position. This branch only removed T-016 (done) and added T-050 in its place; it did not touch T-015/T-003/T-004's rows or try to reconcile the two branches' conflicting edits, since that's O1/O2's call, not O3's, and guessing wrong here would destroy real planning information from one of them | Open, needs O1/O2 reconciliation at merge time |
| 2026-08-27 | O1 | Backlog items that name a specific platform-config mechanism (not just "wire tool X" or "prompt the model to Y") should be schema-validated — checked against the actual `agent.json`/manifest schema and current TrueForge docs — *before* being treated as implementable, not after they're picked up. T-025 sat in §2/§11 as "a config change, not new code" from the project's initial commit through two refills before anyone checked whether `dynamic_sub_agents` (or anything else in the manifest) actually has a model-selection field; it doesn't (§7, 2026-08-27). Same root cause as the T-023 tool-scoping finding, just caught before implementation this time instead of by Qodo after | Adopted going forward — a platform-config task gets a one-line schema check at pickup time, same as a producer-shape task already gets a contract check (§6, 2026-08-26) |
| 2026-08-27 | O1 | `contracts/events.ts`'s `DomainIntel`/`UrlReputation` interfaces don't match the real tool output, found while writing T-024's structured-evidence instructions and deliberately not using them as the target shape. `domain_intel()` (`tools/imports_mcp/domain_intel.py`, merged on `tools/domain-intel`) returns `{domain, rdap: {available, registrar, registration_date, abuse_contact, note}, cert: {available, earliest_seen, age_days, note}, truncated}` — nested under `rdap`/`cert`, with `available`/`note` on each; the contract's `DomainIntel` is flat (`{domain, registration_date, registrar, abuse_contact, cert_issued_at}`) with no `available`/`note`/`truncated` at all and a `cert_issued_at` field that doesn't exist on the real output. `url_reputation()` similarly returns `available`/`threat`/`url_status`/`date_added`/`truncated`/`omitted` that the contract's `UrlReputation` (`{url, listed, tags}`) omits entirely. Same class of bug as Qodo's PR #12 finding #4 (`DetonationResult` invented, not modeled on `detonate.js`) — these two just weren't caught because `tools/domain-intel` hasn't merged into `main` yet, so nothing has type-checked them against each other. Not fixed here: `/contracts` needs 2 approvals (CLAUDE.md) and this is a harness-scoped task | **Update (2026-08-27):** `tools/domain-intel` now merged into `main` (§4, integration entry) — this drift is no longer hypothetical, traced directly to `cockpit/src/missionSource.ts`'s `isDomainIntel` runtime check, which real `domain_intel()` output fails outright (real output has no top-level `registration_date`/`registrar`/`abuse_contact`/`cert_issued_at` at all). Logged as a formal blocker in §5. **Resolved (2026-08-27, PR #19 commit `3f169b3`):** the `DomainIntel` half is fixed — `domain_intel()` now mirrors the four flat fields at the top level alongside the existing nested `rdap`/`cert` sections, satisfying the contract as it already stands, so the `/contracts` PR this row originally called for was never needed. `UrlReputation` is untouched and still narrower than reality, but was never actually breaking (§5's closing note) — no PR pending for it. Removed from §5's active table, full record in §4 |
| 2026-08-29 | O3 | **No decided translation layer from TrueForge's raw turn-stream events to this app's `mission.*` events.** Found while scoping T-056 (reconnect handling): the confirmed live-API mechanics (T-002/§6) cover generic session/turn creation, streaming, `tool.approval_required`, and resume — but nothing anywhere in `contracts/` or PLAN.md defines how the lead agent's actual activity (calling `domain_intel`, detonating a URL, reaching a verdict) becomes a `mission.evidence`/`mission.detonation`/`mission.verdict` event Cockpit can consume. `contracts/events.ts`'s `MissionEvent` union was modeled on §10's architecture *stages*, not on any confirmed wire event TrueForge itself emits. Until this is decided (likely needs O1, since it's harness/prompt-shaped — does the lead agent emit these directly via some structured-output convention, or does something else translate generic tool-call/message events into them?), the real `trueForgeEventSource` `missionSource.ts` has been waiting to swap in since T-050 cannot be built without guessing at it. **Also blocks T-036's live Allow/Deny submission half** (§2/§4) — a real `user.tool_approval` resume has nowhere real to submit to until this exists | Open — needs an O1 (harness) decision on how/whether the lead agent's turn stream maps to `mission.*` events; `resumableStream.ts` (T-056) is ready to consume whatever the answer turns out to be, once decided |
| 2026-08-28 | O1 | **T-035's scope needs correcting before it is implementable — three separate problems, all verified against the live TrueForge schema (§5 carries the evidence).** (a) **"snapshot strategy" is unsupported as written.** TrueForge exposes no snapshot, warm-pool, or prewarm mechanism at all — Daytona has snapshots as a product, but reaching them would mean going around TrueForge, which CLAUDE.md forbids ("don't rebuild what the harness already does"; criterion 04 rewards TrueForge being *central*). This wording should be dropped, not worked around — same class of unverified-at-authoring premise as T-025 (§7, 2026-08-27), caught before implementation this time. (b) **Any warm-sandbox approach must use the one supported lever:** `auto_stop_interval_in_minutes` on the tenant sandbox-provider setting (default 5 minutes idle before Daytona auto-stops). That genuinely addresses trap #4's "never spin a fresh sandbox per detonation", but it is a `PUT /api/v1/settings/sandbox-providers` value, not a repo file — so T-035's deliverable for this half is a documented setting plus whoever applies it, not committed code. (c) **The sandbox-job interface must be defined before T-035 is implemented.** "Sandbox job" is named as an O1 deliverable (§10 repo layout and owners table) and depended on by T-026 ("dispatching O1's sandbox job"), but is specified nowhere — no interface, no shape. Defining it, or deleting the phrase and re-scoping both tasks, is a prerequisite for either. (d) **Daytona credentials are a hard prerequisite for any live verification** — without an api_key there is no sandbox to run in, so T-035 cannot be demonstrated even if written. | Open — needs an O1/O2 decision on (a)-(c) plus a Daytona key for (d); T-035 stays in §2 as a real future task meanwhile, blocked (§5), not disproven |
| 2026-08-29 | O1 | **T-040's wording should be split, because its two halves have opposite answers (§4).** "Session persistence + resume" is **already provided** — TrueForge persists sessions to on-disk SQLite that survives a restart, and exposes `GET /api/v1/sessions` (List sessions), `/sessions/{id}`, `/sessions/{id}/events`, `/sessions/{id}/turns`, `.../turns/{turn_id}/events` and `.../turns/{turn_id}/subscribe`, all verified live. Building any of that ourselves is barred by CLAUDE.md line 148 and would cost criterion-04 credit for wrapping the harness instead of using it. **"Campaign dossier," by contrast, is defined nowhere**: `dossier`/`campaign` appear nowhere in TrueForge's spec, §10's tool table has no dossier tool, no storage format or location is decided, and §17's demo script has no dossier beat — the same "named as a deliverable but never specified" shape as "the Daytona sandbox job" in T-035. Recommend restating T-040 as *consume* the existing session APIs (and deciding whether a campaign dossier is in scope at all, given §17 doesn't show one) rather than *build* persistence | Open — needs an O1/O3 decision on whether a campaign dossier is in scope; the persistence half needs no work and should not be re-picked as if it did |
| 2026-08-29 | O1 | **T-032 (`create_block_rule`) needs a destination decided before it is implementable — the third "named as a deliverable but never specified" task after T-035's "Daytona sandbox job" and T-040's "campaign dossier" (§5 carries the full evidence).** The contract half is genuinely finished: signature, gate configuration and `ProposedActionName` all already exist, so this is *not* an invalid premise like T-025 — the task is real and its remaining work is small. What is missing is only the answer to "a block rule in *what*?" Mailpit exposes no rule/filter endpoint, the repo has no rule store and no `tools/` module that writes to disk, and nothing reads block rules, so any implementation today would invent a storage format *and* a consumer-less store. **Deliberately not decided here.** The most plausible option, if the project wants it, is a self-contained local rule store owned by `imports-mcp` — on the reading that §17's demo beat is the licence *gate* firing, not real enforcement — but adopting that means accepting a write-only tool, and that trade is the project's call to make explicitly, not a gap to fill silently. Recommend either taking that decision on the record, or restating T-032 to name its destination | Open — needs an explicit O1/O2 decision; T-032 stays in §11 flagged ⛔, recoverable, **not** disproven |

---
---

# §9 THE PROJECT

> Forward it anything asking you to click, pay, reset, or approve. It detonates the payload in a
> sandbox, sends three operatives after who's really behind it, and comes back with a verdict and
> a set of actions — **none of which happen until you grant the licence.**

**Codename:** UNIVERSAL IMPORTS — counter to their "Universal Exports" track. Bond's cover was an
import/export firm; counter-intelligence is the half nobody names.

## Why it qualifies (rule 3 + best practice 01)
A judge must see the harness **reach a tool, run code in the sandbox, and stop for a person.**
Ours does all three inside 90 seconds:
- **Reaches:** IMAP mailbox, RDAP, crt.sh, URLhaus, our own MCP server
- **Runs code in the sandbox:** deliberately detonates a stranger's payload — the sandbox is the
  *point*, not a feature bolted on for the rubric. **Say this line in the voiceover**: their own
  ideas slide lists "untrusted code runner"; we want the judge to see *why* the sandbox exists,
  not just that it does
- **Stops for a person:** four irreversible actions, each individually gated

## Why it beats the alternatives
- *Incident responder* is flagged **HERO PROJECT** on their own site — most crowded lane, and the
  baseline version is literally their illustration handed back to them
- *Issue-to-PR* is a dev tool at a dev hackathon; there will be six
- Ours is the only one where the **sandbox is load-bearing** and the person who needs it most
  isn't a developer

## Judging map (six criteria, weighted equally)
| Criterion | How we score |
|---|---|
| Potential impact | Everyone has a parent who's been phished or a company that's lost money to invoice fraud |
| Creativity | Nobody at a dev hackathon builds counter-intelligence |
| Technical excellence | Six harness primitives used because the job needs them, plus a published eval number |
| Use of sponsor tools | TrueForge *is* the runtime; Qodo reviews every PR from day one |
| **Control and safety** | The whole product *is* a control-and-safety story |
| Presentation | Bond theme, amber cockpit, a literal LICENCE REQUIRED gate |

---

# §10 ARCHITECTURE

```
  message arrives (IMAP / maildir watch)
        │
   ┌────▼──────────────────────────────────────────┐
   │ NORMALISE — RFC822 parse, no network          │
   │ headers · SPF/DKIM/DMARC · URLs · attachments │
   └────┬──────────────────────────────────────────┘
        │  TrueForge lead agent
   ┌────▼───────────┬────────────────┬─────────────┐
   │ INFRASTRUCTURE │   IDENTITY     │   HISTORY   │  ← 3 subagents, parallel
   │ RDAP age       │ display-name   │ prior mail  │
   │ cert age       │  vs reply-to   │ from this   │
   │ URLhaus        │ lookalike dom. │ person/dom. │
   │ + DETONATION ──┤                │             │
   └────┬───────────┴────────────────┴─────────────┘
        │
   ┌────▼────────── DAYTONA SANDBOX ───────────────┐
   │ fake portal on sandbox localhost (Range mode) │
   │ headless chromium · redirect chain            │
   │ screenshot · form targets · asks for password?│
   └────┬──────────────────────────────────────────┘
        │
   ┌────▼──────────────────────────────────────────┐
   │ VERDICT + EVIDENCE  →  4 PROPOSED ACTIONS     │
   └────┬──────────────────────────────────────────┘
        │
   ┌────▼── 🔶 FOUR SEQUENTIAL LICENCE GATES ──────┐
   │ TrueForge pauses on each approval-marked tool │
   │ call and shows the JSON request. We configure │
   │ this in agent.json — we do not build it.      │
   │  1. quarantine        → [Allow] [Deny]        │
   │  2. notify impersonated → [Allow] [Deny]      │
   │  3. block rule        → [Allow] [Deny]        │
   │  4. abuse report      → [Allow] [Deny]        │
   └────┬──────────────────────────────────────────┘
        │
   execute granted · write campaign dossier · speak the verdict
```

## 🔴 The architecture catch that would have cost us an afternoon
**Daytona is remote. It cannot reach a Range running on `localhost` on your laptop.**
→ The fake portal runs **inside the sandbox itself.** Zero networking, works offline, works on a
judge's machine, and it's more honest — payload and browser isolated together.

## Repo layout — you own a folder, full stop
```
/contracts     shared types + fixture event stream · PR needs 2 approvals
/harness       O1 — TrueForge config, agent.json, sandbox job, subagents
/tools         O2 — imports-mcp server, normaliser, intel APIs, gated actions
/cockpit       O3 — the UI
/mission       O4 — Range fixtures, skills, prompts, eval harness
PLAN.md        this file
CLAUDE.md      the rules Claude Code auto-reads
```

## MCP tool surface (`tools/imports-mcp`)
| Tool | Returns | Gated |
|---|---|---|
| `parse_message(id)` | headers, auth results, URLs, attachment hashes | no |
| `domain_intel(domain)` | RDAP age/registrar/abuse contact; crt.sh cert age | no |
| `url_reputation(url)` | URLhaus verdict + tags | no |
| `correspondence_history(address, domain)` | prior contact count, first/last seen, domains used | no |
| `detonate(url)` | redirect chain, screenshot ID, form analysis | no |
| `quarantine(message_ids)` | — | ⚠️ **yes** |
| `notify_impersonated(address, evidence)` | — | ⚠️ **yes** |
| `create_block_rule(pattern)` | — | ⚠️ **yes** |
| `file_abuse_report(domain, evidence)` | — | ⚠️ **yes** |

**Every tool response under ~2KB. Never return raw HTML to the model.**

## Owners
| | Role | Folder | Backup |
|---|---|---|---|
| **O1** | Harness — TrueForge config, agent.json, sandbox job, subagents, approvals, sessions | `/harness` | O2 |
| **O2** | Tools — MCP server, normaliser, intel APIs, the four gated actions | `/tools` | O1 |
| **O3** | Cockpit — the UI, **and the four iPads** | `/cockpit` | O4 |
| **O4** | Mission — Range, skills, prompts, evals, Qodo trail, README, video, blog. **Tiebreaker** | `/mission` | O3 |

---

# §11 BACKLOG — refill NEXT UP from here

> Claude pulls from the top of the current slice when a task completes. Move an item here to
> §2 rather than inventing a new one, unless §8 says the plan changed.

## Slice 1 — ugly vertical slice (Day 1)
*One hardcoded fixture → parse → one subagent → hardcoded verdict → one approval gate → cockpit shows it. It will be hideous. It must run before anyone sleeps.*
*(T-011, T-012 pulled into §2 NEXT UP — nothing left here for Slice 1.)*

## Slice 2 — intelligence (Day 2)
- **T-022** [O2] `correspondence_history` — IMAP search for prior contact
- **T-026** [O2] `detonate` MCP wrapper dispatching O1's sandbox job

> Note (2026-08-27, O1): T-025 removed — disproven as an invalid task premise, not deferred. See §7/§8 for why; it will not be re-added to this list.

## Slice 3 — the product (Day 3) ← *everything after this is cuttable*
- **T-030** [O2] `quarantine` over IMAP
- **T-032** ⛔ [O2] `create_block_rule` — **BLOCKED, do not pick** — the rule's destination is undecided and nothing consumes block rules (§5/§8). Original wording preserved: *"`create_block_rule`"*; the contract, gate config and `ProposedActionName` already exist, so this is a scope question, **not** an invalid premise

> Note (2026-08-28, O1): T-031 removed — completed and merged into `main` (§4, PR #29), not just picked; it was an explicit, disclosed cross-owner takeover (§4 entry has the reasoning), not a reassignment of the rest of this list. T-030/T-032 untouched — still `[O2]`, still unstarted (T-033 was also untouched when this note was written; it has since been completed and merged — see §4 and the 2026-08-29 O1 note in this section).
> Note (2026-08-29, O1): T-033 removed — completed and merged into `main` (§4, PR #40, `71107ad`), same disclosed cross-owner takeover as T-031, not a reassignment of the rest of this list. T-030/T-032 untouched — still `[O2]`, still unstarted; two of the four T-034 gate-target tools now exist.
> Note (2026-08-29, O3): T-036 removed — picked into §2 NEXT UP (found sitting here unpicked while selecting O3's next task after T-056). Only its presentation half is unblocked (§2/§4/§8 have the full correction) — the live-submission half is blocked on the same undecided TrueForge→`mission.*` translation layer T-056 surfaced. T-030/T-032 untouched — still `[O2]`, still unstarted (T-033 was also untouched when this note was written; it has since been completed and merged — see §4 and the 2026-08-29 O1 note in this section).

## Slice 4 — depth (Day 4)
- **T-040** ⚠️ [O1] Session persistence + resume — **persistence half needs no work: TrueForge already provides it (on-disk SQLite + `GET /api/v1/sessions` and friends, verified live — §4/§8). Do not re-pick it as if it were unbuilt.** What's left is only the undefined "campaign dossier" scope question (§8)
- **T-041** [O1] Error paths: sandbox dies, MCP times out, model returns garbage, redirect loop
- **T-042** [O4] Eval harness — 20 phish (phishing_pot) + 20 ham (SpamAssassin/Enron), report accuracy **and false-positive rate**
- **T-043** [O3] Spoken verdict — Web Speech API, ~1h
- **T-044** [O3] **UI polish block — book the afternoon, nothing else in it. This is the iPad**
- **T-045** [O2] SQLite caching so repeated missions don't re-hit crt.sh

## Cockpit (O3, from join day)
- **T-055** ⛔ Dossier view — past missions, resumable, campaign recognition — **BLOCKED (§5)**, moved back here from §2 (2026-08-29): its data dependency (persisted past missions) doesn't exist yet — needs T-040 (O1) or a confirmed TrueForge session-history API first

## Mission (O4, from join day)
- **T-060** Range: `docker compose up` → Mailpit + fake portal + `seed.sh` — **merged 2026-08-28 (PR #22, `f9c18f6`); §5 has the T-022 mailbox-protocol decision this creates.** (No §4 entry written for T-060 itself here — out of scope for this correction; O4's own PR carries that record.)
- **T-061** 20 fixtures: **15 malicious, 5 legitimate that must NOT trip it**
- **T-062** `fake-portal/` — **fictional brands only** ("Northgate Trust", "Meridian Courier")
- **T-063** Skills: `triage-a-message`, `read-a-detonation`, `propose-actions`, `explain-to-a-human`
- **T-064** Qodo PR triage — daily, nothing merges unreviewed
- **T-065** README tested by a **clean clone on someone else's machine**
- **T-066** Write-up: how the agent uses TrueForge, primitive by primitive
- **T-067** Demo video — **4 hours on Day 5**, rough cut Day 4
- **T-068** Blog post (Field Report prize — one writer)
- **T-069** Day 4 cross-brief — each owner explains their area to the other three (rules 12 & 13)
- **T-070** Secret sweep — no tokens in repo, **no tokens visible in the video**
- **T-071** Daily social posts tagging WeMakeDevs + TrueFoundry

---

# §12 THE TEN WAYS WE WILL WASTE HOURS

Read aloud at kickoff. Re-read at the Day 3 standup.

| # | Trap | Cost | Rule |
|---|---|---|---|
| 1 | **MCP transport mismatch** (stdio vs HTTP/SSE) | 4h | Read the cookbook example **before** writing the server |
| 2 | **The chromium install loop** | 6h | Timebox 3h → text-mode fallback |
| 3 | **Gmail OAuth** — `gmail.modify` is restricted: consent screen, possible app review | 8h | **IMAP + app password.** 20 minutes. Decided matter |
| 4 | **Verifying DKIM ourselves** — DNS + canonicalisation | 2 days | Read the `Authentication-Results` header |
| 5 | **Dumping HTML into model context** | money + accuracy | Summarise inside the tool |
| 6 | **Fresh sandbox per detonation** — 2–5 min cold start | kills the demo | Snapshot or keep warm |
| 7 | **Tuning prompts before the pipeline runs** | 1 day | Bytes flowing end to end first |
| 8 | **Polishing UI against imaginary data** | rework | Build against `contracts/fixtures/` |
| 9 | **Trying to build a real security product** | the week | One narrow job, three-minute demo |
| 10 | **No contracts → merge hell on Day 3** | 1 day | 90 minutes on Day 0 buys the week |

## Smaller catches that will surprise us mid-week
- ⚠️ **Local fixtures have no `Authentication-Results` header** — it's added by the *receiving*
  server. Hand-write realistic ones or the identity subagent has nothing to read on Day 1
- ⚠️ **URLhaus is malware-focused.** Most phishing URLs aren't listed. **"Not listed" ≠ safe.**
  Never build a demo beat on a URLhaus hit — you'll get a shrug on camera
- ⚠️ **crt.sh is slow and 502s.** 5s timeout, cache to SQLite, never block a mission
- ⚠️ **Many ccTLDs have partial/no RDAP**, and GDPR redacts abuse contacts. *"Registration date
  not published"* is a valid finding, not a crash
- ⚠️ **Extract URLs with `lxml`, not regex.** Regex on HTML costs an evening
- ⚠️ **href ≠ anchor text is the highest-signal free feature.** Three lines of code
- ⚠️ **Don't base64 screenshots into tool results.** Write the PNG, return an ID
- ⚠️ **Never fire a real abuse report at a real registrar during testing.** Range mail server only
- ⚠️ Charset lies are common — wrap decoding in try/except, fall back to `latin-1`

---

# §13 DATA SOURCES

## Tier 1 — The Range (ships in the repo; **this is what judges run**)
`range/` → Mailpit + fake portal + `seed.sh`. 20 fixtures: 15 malicious across varied tactics
(credential phish, invoice fraud/BEC, CEO impersonation, fake courier, OTP theft, subscription
scam, malicious attachment) and **5 legitimate that must not trip it.**

*Rule 5 says judges must be able to run your code. Every other security submission will be
undemonstrable — "trust my video, I can't show you my inbox." A judge can clone ours and watch
the whole mission offline on their own laptop.*

## Tier 2 — Public corpora (**evals only, never the demo** — their URLs are years dead)
| Source | Gives | Where |
|---|---|---|
| **phishing_pot** | Raw `.eml`, actively collected | `github.com/rf-peixoto/phishing_pot` |
| **Nazario corpus** | ~4.5k hand-screened phish, 2015–2021 | `monkey.org/~jose/phishing/` |
| **SpamAssassin public corpus** | **Ham — our false-positive baseline** | `spamassassin.apache.org/old/publiccorpus/` |
| **Enron** | Legitimate business mail baseline | `cs.cmu.edu/~enron/` |

**The ham matters more than the phish.** An agent that flags everything is worthless, and
false-positive rate is the number that makes the write-up credible.

## Tier 3 — Live (one rehearsed shot in the video)
Our own spam folders → throwaway account. Unambiguously ours (rule 6). **Never committed.**

## Intel APIs
| API | Key | Note |
|---|---|---|
| RDAP (`rdap.org`) | none | age, registrar, abuse contact |
| crt.sh | none | *"cert issued 14 hours ago"* is a killer signal |
| URLhaus | free, `auth.abuse.ch` | weak signal only |
| ~~PhishTank~~ | ❌ | Registration closed since 2020 |

## Safety rules (put these in the README — judges notice)
1. Sandbox holds **no credentials**, no route back to our systems
2. **Never render remote images** — tracking pixels confirm a live address
3. Attachments: static unpack + hash lookup only. **No execution** in v1
4. Fixtures stored de-fanged (`hxxp://`), links rewritten to the Range
5. No real brand assets — fictional brands only
6. Live samples never committed

---

# §14 BUILD ORDER & SCHEDULE

| Slice | Runs end to end | Day |
|---|---|---|
| **1** | Hardcoded fixture → parse → one subagent → verdict → one gate → cockpit. **Ugly** | 1 |
| **2** | Three real subagents, real intel, real verdict with evidence | 2 |
| **3** | **Detonation. Four actions. Per-action grant/deny. Actions execute** ← *the product* | 3 |
| **4** | Dossier, evals, voice, error paths | 4 |

| Day | Focus | 10 PM gate |
|---|---|---|
| **0 · Sun 24** | Spikes + scaffold + Qodo + contracts | Spikes answered, logged in §6 |
| **1 · Mon 25** | Slice 1, ugly | 40-second recording of the ugly slice |
| **2 · Tue 26** | Slice 2 | Correct verdict on an unseen fixture |
| **3 · Wed 27** | Slice 3. **Nothing new after 10 PM** | Full mission runs live on the call |
| **4 · Thu 28** | Dossier, evals, **UI polish (iPad day)**, cross-brief | Eval number exists, rough cut recorded |
| **5 · Fri 29** | **FREEZE 12:00 IST.** Video (4h), README, write-up, blog | Everything but submit is done |
| **6 · Sat 30** | Final Qodo pass, clean run, **submit 18:00 IST** | Submitted with 6h spare |

**Standups:** 10:00 AM IST (15 min, plan) · 10:00 PM IST (30 min, integrate + run end to end).
If it doesn't run end to end, that's tomorrow's only priority.

---

# §15 COMMIT & PR DISCIPLINE — *this is the Best Code Quality track*

> *"Judges read your pull request history, so the review trail is the evidence. A repo with a
> single pull request opened an hour before the deadline will not win this one."*

- **`main` is protected. No direct pushes. Ever.**
- Branches: `<area>/<short-description>` → `tools/rdap-client`, `harness/spike-chromium`
- **PRs under ~400 lines.** A 2,000-line PR gets a useless review from Qodo *and* from humans
- **Every PR gets a Qodo review before merge.** Fix real findings; if you disagree, **say why in
  a comment** — that reply is part of the evidence
- `main` stays green. Break it, fix it before you sleep
- **Squash-merge**, so `main` reads as one clear change per PR
- Commit body explains **why**, not what — the diff already says what
- **No secrets, ever.** `.env.example` only. If a key touches a commit, **rotate it** — deleting
  the commit is not enough

**Conventional commits:**
```
feat(tools): add RDAP domain age lookup
fix(harness): cap redirect chain at 10 hops
docs(plan): record spike 1 outcome
chore(range): add 3 credential-phish fixtures
spike(harness): test chromium in daytona sandbox
```
Types: `feat` `fix` `docs` `chore` `test` `refactor` `spike`

**Rule 11 — AI disclosure is mandatory.** Every AI-assisted commit carries:
```
Assisted-by: Claude Code
```
Never add it to a commit written entirely by a human — the disclosure has to be accurate to be
worth anything.

**Rules 12 & 13 — we must understand our own code.** *Projects entirely generated by AI without
meaningful participant contribution, verification, or technical understanding may be rejected.*
**Never merge a PR you cannot explain line by line.** Day 4 cross-brief is booked, not optional.

---

# §15a QODO — what it's for, and what it isn't

**Yes, we use it. Install day one, then forget it exists.**

## Why, precisely
Judging criterion 04, scored on **every** submission regardless of track:
> *"Is TrueForge central to the project rather than a thin wrapper around a model, **and did Qodo
> review the pull requests on the way there?**"*

Six criteria, weighted equally. Skipping Qodo means walking into criterion 04 with half the
question unanswerable — on the UI track, on the TrueForge track, on all of them.

## What it costs
~10 minutes of setup. Then nothing. It comments on pull requests by itself.

## What it is
A dev tool. It reviews our pull requests with context from the whole repository, flags bugs and
logic gaps, and suggests fixes. **It never touches the product**, never appears in the
architecture, never runs at demo time.

## What it is NOT
- Not part of the agent
- Not something we build against or integrate with
- **Not a track we're chasing.** Best Code Quality is one Mac Mini split four ways. Best UI is an
  iPad *per member*. Rule 14: one team takes only one track. We aim at UI

## The one non-negotiable
**It cannot be retrofitted.** Their site: *"A repo with a single pull request opened an hour
before the deadline will not win this one."* Install before the first real commit, open PRs
instead of pushing to main, let it review, fix the real findings, and reply in a comment when you
disagree — that reply is part of the evidence too.

---

# §16 SUBMISSION CHECKLIST (rule 9)

- [ ] Public repo, readable and runnable (rule 5)
- [ ] README with setup steps, **verified by a clean clone on someone else's machine**
- [ ] Demo video ~3 minutes
- [ ] Write-up: what the agent does + **how it uses TrueForge**, primitive by primitive
- [ ] Blog post published and linked (Field Report prize)
- [ ] Agent runs on TrueForge, harness visibly doing real work (rule 3)
- [ ] Qodo installed at the start, PR trail reviewed all week
- [ ] Only our own tools/data/accounts; **no keys or personal data in repo or video** (rule 6)
- [ ] All code written 24–30 Aug (rule 7)
- [ ] AI disclosure complete (rule 11)
- [ ] All four can explain the architecture (rules 12, 13)
- [ ] TrueForge repo starred by all (Calling Card draw)
- [ ] Daily posts tagging WeMakeDevs + TrueFoundry (Radio Traffic)

**Track note (rule 14):** one team can only take one of the three judged tracks. **Best UI awards
an iPad to every member** — per person, the richest track for a team of four, and most teams
forfeit it by shipping the stock chat window.

---

# §17 THE DEMO (3:00) — written before the code, on purpose

| Time | Shot |
|---|---|
| 0:00–0:20 | An email that would fool you. "Your CFO wants an urgent transfer" |
| 0:20–0:45 | Forward it. Agent accepts. Three evidence lanes light up in parallel |
| 0:45–1:25 | **Sandbox detonation** — redirect chain resolving, then the screenshot of the fake portal. It asks for a password and posts it elsewhere |
| 1:25–1:50 | Evidence: domain 2 days old · cert issued yesterday · reply-to is a lookalike · your CFO has mailed you 214 times, never from here |
| 1:50–2:20 | 🔶 **LICENCE REQUIRED**, four times over. Each gate shows the literal request before it runs. Allow, allow, **deny**, allow — the denial is the shot that proves the gate is real |
| 2:20–2:40 | Granted actions execute for real. Quarantined. The real CFO warned |
| 2:40–3:00 | A phone. Plain English, spoken: *"This is fake. It is not your bank. Do not click it."* Cut to the harness diagram |

**If it isn't in this script, it isn't a priority.** Every proposed feature answers: *which second
does this appear in?*

---

# §18 HONEST ODDS

Build ≈ **45–60 engineer-hours**. Four people × six days × 5 realistic hours ≈ 120.
**~2× headroom**, which is right — half of hackathon time goes to integration, debugging, video.

| Outcome | Likelihood |
|---|---|
| Something working and submittable | ~90% |
| Slices 1–3 done by end of Day 3 | ~85% |
| **With screenshot detonation** | ~60% — hostage to T-001 |
| With text-mode fallback | ~95% |
| Plus dossier, evals, voice, polished UI | ~50% |

> **Every hour we lose this week will be lost to refusing a fallback, not to the work being too hard.**
